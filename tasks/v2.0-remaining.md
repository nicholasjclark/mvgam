# mvgam v2.0 — remaining todos

Source: milestone 1 (#109 feature tracker, #117 trend roadmap),
pending TaskList items (#106, #107, #108, #187), and direct user
asks during the brms-integration branch work.

Listed in suggested landing order: cheap diagnostic sweeps first,
big surface-extending work in the middle, polish last. Each item
has touch points, definition of done, and an effort estimate.

---

## 1. Observation-level brms specials sanity check

**Source:** #109 ("Switch to full support for all families").

**Goal.** Confirm every brms observation-level special still
compiles + samples on `feature/brms-integration`. The branch
overhauled the formula plumbing and prediction stack but never
exercised the full special set. Some of these are likely already
working; some are likely broken silently. We want a quick
diagnostic sweep before bigger work piles on top.

Specials to exercise, one tiny fixture each:

| Special | What it does | brms reference |
|---|---|---|
| `mm()` | multiple membership grouping factors | `?brms::mm` |
| `me()` | measurement error on predictors | `?brms::me` |
| `cs()` | category-specific effects (ordinal) | `?brms::cs` |
| `se()` | known sampling error on the response | `?brms::se` |
| `cens()` | left / right / interval censoring | `?brms::cens` |
| `trials()` | number of trials for binomial / beta-binomial | `?brms::trials` |
| `car()` | spatial conditional autoregression | `?brms::car` |
| `mi()` | missing predictor imputation | `?brms::mi` |

`mi()` on the observation side is left to brms (no mvgam
rejection). The constraint from item 12 of #109 lives entirely
on the latent side: `trend_formula` must reject every brms
`formula_ad` special EXCEPT `mi()`. The trend-side validator
walks the formula AST via `formula_rhs_function_names()` so
user variable names that share a substring with an addition-
term name (e.g. `defense`, `se_x`) cannot false-positive.

**Touch points:**
- New `tests/testthat/test-brms-observation-specials.R` — one
  block per special. Each block builds a small dataset, calls
  `make_stancode(fit_dummy)` style assertions, and (where
  reasonable on CI) actually samples a 200-iter fit and runs
  `posterior_predict()` to verify the prediction path.
- Anything that breaks gets a corresponding fix in the relevant
  R file. Likely suspects:
  - `R/formula_handling.R` for the formula traversal / split
    that strips specials before passing to the trend submodel.
  - `R/posterior_predict.mvgam.R` for the family / dpar / cens
    branching.
  - `R/log_lik.mvgam.R` for censoring-aware likelihood.

**Definition of done.** All six specials compile and sample on
the in-package test fixtures. Failing cases either fixed or
documented as deferred with a clear reason in `?mvgam_families`.

**Effort:** 0.5 to 1.5 days. The unknown is how much breakage
the sweep surfaces.

---

## 2. Port `irf.mvgam` and `fevd.mvgam` to the new data layout

**Source:** direct user ask during this conversation; not yet
in the task tracker.

**Goal.** Both files still call
`mcmc_chains(object$model_output, "A")` and
`mcmc_chains(object$model_output, "Sigma")` (see
`R/irf.mvgam.R:88-89` and `R/fevd.mvgam.R:84-85`). The
`model_output` slot was renamed to `fit` during the brms-
integration rewrite, so these surfaces silently no longer work
on current fits.

**Touch points:**
- `R/irf.mvgam.R` and `R/fevd.mvgam.R` — replace
  `mcmc_chains(object$model_output, X)` with
  `posterior::as_draws_matrix(object$fit)[, grep("^X\\[", ...)]`
  using the per-parameter extraction patterns already in
  `R/sample_innovations.R::extract_indexed_array_2d`.
- `R/mvgam_irf-class.R` and `R/mvgam_fevd-class.R` — verify
  `summary()` / `print()` / `plot()` methods still work on the
  ported output shape.
- `R/stability.R` — uses the same VAR coefficient extraction
  pattern; check and port in the same pass.

**Tests:**
- Reactivate the existing IRF / FEVD example blocks in
  `R/irf.mvgam.R` and `R/fevd.mvgam.R` (currently `\donttest{}`).
- Add `tests/testthat/test-irf-fevd.R` running both on the VAR
  fixture from `tests/local/`. Assert posterior IRF / FEVD draws
  have the expected dims and sum to 1 (FEVD) per horizon.

**Definition of done.** `irf(fit)`, `fevd(fit)`, `stability(fit)`
all return populated objects on a current VAR fit, plotting and
summary methods still render, examples pass with `\dontrun{}`
removed.

**Hierarchical VAR extension (item 32 of #109).** When the fit
has grouped / hierarchical VAR structure (`L_omega_global` /
group-specific `A`), the same ported surfaces should expose:
- `irf(fit, group = ...)` returning group-specific IRFs.
- `fevd(fit, group = ...)` analogously.
- A "global" pooled IRF / FEVD computed from the population-
  level VAR coefficient matrix.

Treat the grouped case as a second pass after the basic port
lands; it's the same code path with one extra dimension.

**Effort:** 1.5 to 2 days including the hierarchical pass.

---

## 3. Hierarchical / shared AR coefficients (Step 3b shipped, ARR2 dropped)

**Source:** #109 ("Added flexibility for latent processes"); also
linked issue #35 for the sharing surface.

**Status (2026-06-09):** `coef_sharing` for `AR()` shipped in
task #193. The ARR2 prior surface was implemented in #192 / #194
but **removed** after the recovery fit showed the prior could
not be made identifiable in a state-space context (see
"ARR2 decision" below). The surface left to users is:

```r
AR(p = ...,
   coef_sharing = c("none", "shared", "hierarchical"))
```

**Stan emission per `coef_sharing` mode** (verified via
contracts in `tests/testthat/test-stancode-standata.R`):

| coef_sharing   | Sampled parameters with priors |
|----------------|--------------------------------|
| none           | `ar{lag}_trend` per series |
| shared         | `ar{lag}_shared` (one per lag) |
| hierarchical   | `mu_ar{lag}_trend`, `sigma_ar{lag}_trend`, `ar{lag}_trend` |

All variants synthesise `ar{lag}_trend[j]` in transformed
parameters, so downstream forecasting / IRF / FEVD / summary
keep their existing read paths.

**End-to-end MCMC verification of the shipped surface**
(2026-06-09, 4 series x 60 timepoints, AR(2) truth
`phi = c(0.5, 0.2)`, 2 chains x 600 iter):

| Variant                     | Divergences | R-hat range | Truth recovery |
|-----------------------------|-------------|-------------|----------------|
| `default + hierarchical`    | 35/600 (6%) | 1.02-1.17   | Yes (`mu_ar1` 0.51, `mu_ar2` 0.15, `sigma_ar*` correctly small) |

The hierarchical fit recovers cleanly modulo a divergence rate
that drops under `adapt_delta = 0.95+` (a sampler tuning knob,
not a parameterisation defect).

### ARR2 decision (dropped)

Two attempts at implementing the Sælensminde et al. 2024 ARR2
prior on AR coefficients were made and both abandoned:

1. **State-space simplification** (cancelling `sigma_trend`
   via the stationarity identity `var_y = sigma² / (1 - R²)`):
   `R2_trend` posterior was multimodal, R-hat 1.89-2.26.
2. **Restore the paper's full form** with `var_y_trend` as a
   Stan data constant computed from the empirical response
   variance: R-hat 2.49 on the parameter set, 49 % treedepth
   saturation, AR coefficient posteriors centred on the wrong
   sign relative to truth.

**Root cause.** The ARR2 prior puts `phi_z ~ std_normal()` on
the non-centred AR coefficient z-scores. For AR(1),
`phi_1 = sqrt(R²) * phi_z`, which gives `|phi_1| > 1`
(non-stationary AR) whenever `|phi_z| > 1`. The paper assumes
direct AR observations where the likelihood dominates the
prior and pulls `phi` back into the stationary region; in
mvgam's state-space setting the observation noise weakens the
likelihood signal enough that the sampler explores the
non-stationary region and diverges. The R²-based
reparameterisation does not impose any analytic stationarity
constraint, so adding the paper-form `var_y` constant only
pins the scale of the coefficient magnitudes, not their
stationarity.

**Action taken.** Reverted the `prior = "arr2"` option from
`AR()`. `coef_sharing` (Step 3b) is unaffected and remains
shipped. Files reverted:

- `R/trend_system.R::AR()` — `prior` and `arr2` args removed;
  three constructor-side rejection paths (sparse-lag + arr2,
  arr2 + hierarchical, arr2 without prior) removed; roxygen
  trimmed back to the `coef_sharing`-only surface.
- `R/stan_assembly.R` — `build_arr2_ar_stanvars()` removed;
  `build_ar_coef_stanvars()` signature simplified to drop
  `prior_type`, `arr2_spec`, and `var_y`; `generate_ar_trend_stanvars()`
  no longer threads `var_y` into the dispatcher;
  `extract_trend_stanvars_from_setup()` no longer threads
  `response_name` into `data_info`.
- `R/priors.R` — entire ARR2 block (`normalise_arr2_spec`,
  `resolve_arr2_prior_strings`, `arr2_concentration_literal`,
  `arr2_family_name`, `arr2_family_class`,
  `arr2_link_transform`, `warn_arr2_var_y_fallback`,
  `compute_arr2_var_y`, `arr2_coef_expr`, `make_arr2_stanvars`,
  `resolve_arr2_prior`, ~600 lines) deleted; `R2_trend`,
  `psi_trend`, `phi_z_trend` rows removed from
  `common_trend_priors`; ARR2 branches removed from
  `get_parameter_type_default_prior`.
- `R/sample_innovations.R::hidden_unrotated_factor_pars()` —
  reverted to A_trend-only handling.
- `R/trend_system.R::generate_ar_monitor_params()` —
  ARR2 branch removed.
- `tests/testthat/test-stancode-standata.R` — 12 ARR2
  contract tests, 6 helper-level tests, and 1 default-prior
  regression test removed.

**Out of scope (permanent decision).** Discontinuous / sparse-lag
VAR (e.g. `VAR(p = c(1, 4))`). Reason: VAR's stationary joint-
distribution initialisation (Heaps 2022) assumes consecutive
companion-form structure, so the sparse case has no companion-
form analogue with the same identified stationary covariance.
The `VAR()` constructor already rejects vector `p` with a wording-
corrected error pointing users at `AR(p = c(...))`.

**Pending under this item.** Cached local fixtures + recovery
test for the hierarchical AR variant
(`val_mvgam_hier_ar1`), once `tests/local/build_fixtures.R`
gets its next refresh.

---

## 4. Custom families

### 4a. `tweedie()` — shipped

**Status (2026-06-09):** shipped in task #195 as a
`brms::custom_family` registered on the main `mvgam()` surface.
The Stan log-density is the compound Poisson-gamma decomposition
of Spinkney (Stan Forums) adapted from the open-AIMS/tweedie
implementation, with strict-positivity guards on `mu` and `mphi`
and the `mtheta` interval tightened from `[1, 2]` to
`[1.001, 1.999]` to avoid the Poisson and Gamma boundary
singularities.

**API:** `tweedie(M = 30L)`. Three distributional parameters
(`mu`, `mphi`, `mtheta`) with log / identity / identity links.
`M` is the Poisson-gamma series truncation passed as Stan data.

**Where the code lives:**
- `R/families.R` — `tweedie()`, `make_tweedie_stanvars()`,
  `tweedie_stan_funs()` (the Stan function block),
  `attach_family_stanvars()`, `log_lik_tweedie()`,
  `check_tweedie_truncation()` runtime diagnostic, and
  `resolve_family_name()` helper for the customfamily naming
  quirk (brms stores the user-visible name on `$name`, with
  `$family = "custom"`).
- `R/make_stan.R` — auto-attaches `attr(family, "mvgam_stanvars")`
  onto the obs setup only, so the trend submodel doesn't get a
  duplicate functions-block / M-data stanvar pair that would
  collide at the final `c.stanvars` merge.
- `R/stan_assembly.R` — `generate_combined_stancode_and_data()`
  now feeds both the obs-side stanvars and the trend stanvars
  into `brms::make_standata()` so the family's data inputs
  (e.g. `M`) make it through.
- `R/posterior_predict.R` — `tweedie` branch in
  `sample_from_family()` (via `mgcv::rTweedie`), plus
  `tweedie = c("mphi", "mtheta")` in `get_family_dpars()`.
- `R/posterior_epred.R` — `tweedie` branch returns
  `family$linkinv(linpred)` (exact: `E[Y | mu, phi, theta] = mu`
  across the full `theta in (1, 2)` range).
- `R/log_lik.mvgam.R`, `R/posterior_predict.R`,
  `R/posterior_epred.R` — `family_name <- family$family` reads
  replaced by `resolve_family_name(family)`.
- `R/how_to_cite.R` — three new `reference_db()` entries
  (`jorgensen_tweedie`, `dunn_smyth_tweedie`,
  `aims_tweedie_brms`) gated by `uses_tweedie_family()`
  predicate.

**Statistical / numerical decisions:**
- **mgcv over the CRAN `tweedie` package** for both `ldTweedie`
  (log-density) and `rTweedie` (random draws): mgcv is already
  in Imports, returns log densities directly, and removes the
  `log(pmax(dtweedie, eps))` two-step that would silently bias
  LOO upward where the raw density underflows.
- **Log link fixed.** Identity / inverse links cannot guarantee
  the `mu > 0` requirement of the compound Poisson-gamma when
  the linear predictor is driven by a latent stochastic trend;
  rejected at the constructor.
- **Series truncation M.** `M = 30` is enough when the implied
  Poisson rate `lambda = mu^(2 - theta) / ((2 - theta) * phi)`
  stays well below `M`. `check_tweedie_truncation()` warns when
  `max(lambda)` at the posterior mean exceeds `0.7 * M`. Default
  is generous for typical-magnitude data; users with very small
  `phi` or `mtheta` close to 2 should bump `M` and refit.

**Verification (2026-06-09):**
- Stan-side state-space recovery on simulated Tweedie + AR(1):
  `mphi` 1.04 vs truth 1.0; `mtheta` 1.53 vs truth 1.5;
  R-hat <= 1.01; 1 divergence in 600 transitions.
- `posterior_epred` / `posterior_predict` / `log_lik` all run
  on the SS fit; `log_lik` is finite for every cell.
- Tweedie vs Poisson LOO on the same overdispersed simulated
  data (var/mean = 1.48): Tweedie wins by 494 ELPD units,
  28.4 standard errors.
- Per-observation log_lik cost ~2.2 ms (600 draws x 300 obs ~
  0.6 s total).
- 62 contract tests in `tests/testthat/test-tweedie-family.R`
  pass.

### 4b. `nmix()` and `occ()` — folded into `jsdgam()` (Step 5)

Original Step 4 also planned `nmix()` and `occ()` as standalone
custom families with their own auto-built `trend_map`. Per user
decision (2026-06-09): both are pushed into the `jsdgam()`
ecology wrapper (Step 5) instead of standing alone on the main
`mvgam()` surface. The trend-map auto-construction, detection-
history layout, and detection-vs-abundance parameter separation
are all ecology-specific and have no obvious cross-domain use.
Royle-Nichols becomes `nmix(type = "royle_nichols")` once
`nmix()` lands inside `jsdgam()`.

This drops Step 4 from "6-8 days, three full families + Royle-
Nichols + cross-cutting predict / pp_check / summary surfaces"
to ~1.5 days (the Tweedie work that just landed).

**Useful patterns to reuse when nmix/occ are tackled inside
`jsdgam()` (recorded here so the wiring isn't rediscovered):**

- **Custom-family plumbing already exists.** `R/families.R`
  has `tweedie()` as a worked example of a brms
  `custom_family` wired into mvgam: `attach_family_stanvars()`
  in `R/make_stan.R` auto-merges the family's stanvars onto
  the obs setup only (the trend submodel must not see them,
  or `c.stanvars` errors on duplicate names); the standata
  merger in `R/stan_assembly.R::generate_combined_stancode_and_data()`
  also has to include obs-side stanvars so any data inputs
  flow through. Both fixes are in place.
- **Family-name dispatch needs `resolve_family_name(family)`,
  not `family$family`.** brms stores `family$family = "custom"`
  on any customfamily and the user-visible name on
  `family$name`. All downstream dispatchers in mvgam
  (`compute_family_epred`, `dispatch_log_lik`,
  `sample_from_family`, `summary.mvgam`, `glance`, `tidy`,
  `how_to_cite`) already route through `resolve_family_name()`.
  New families just inherit it.
- **Custom dpars must be registered in three places.**
  `R/posterior_predict.R::get_family_dpars()` (for log_lik to
  extract them); `R/summary.mvgam.R::match_family_pars()` (for
  the "Further Distributional Parameters" block); and
  `R/index-mvgam.R` `obs_family_pattern` (for tidy / coef /
  parameter classification). Missing any one silently drops
  the parameter from that surface. Tweedie added `mphi` /
  `mtheta` to all three.
- **nmix / occ change the latent structure, not just the
  observation density.** Unlike Tweedie, they need an
  auto-built `trend_map` that links repeat visits / detection
  histories to a shared latent abundance / occupancy state.
  Extend `R/validations.R::normalise_trend_map()` rather than
  routing through the existing `trend_map = ...` user
  argument; document the visit/site shape required in the
  jsdgam roxygen.
- **Likelihood evaluation strategy.** For Tweedie we deferred
  R-side log_lik to `mgcv::ldTweedie` (already in Imports,
  returns log densities directly, avoids the
  `log(pmax(density, eps))` floor that biases LOO upward).
  For nmix / occ no obvious mgcv equivalent exists, so the
  log_lik must marginalise latent N / occupancy in R-side
  code. Plan to write a per-family `log_lik_<family>` helper
  (the existing dispatch in `R/log_lik.mvgam.R::dispatch_log_lik`
  picks them up by name) and a numerically-stable
  marginalisation using `matrixStats::logSumExp`. Confirm
  the upper-bound `K` for the latent-N sum is exposed as a
  family arg like Tweedie's `M`, and that
  `check_<family>_truncation()` warns when the posterior
  pushes the implied latent state near `K`.

---

## 5. `jsdgam()` ecologist-facing wrapper

**Source:** Heaps factor-architecture plan §"jsdgam translation",
plus the direct user ask in this round.

**Goal.** A wrapper around `mvgam()` that exposes domain-friendly
aliases for joint species distribution model workflows. The
underlying Stan machinery is the `loadings_prior` infrastructure
shipped in task #166. No new Stan code, but real R-side work for
the translation, validation, and JSDM-specific defaults.

**Surface:**

```r
jsdgam(
  formula        = species ~ env_covariates,
  data           = sites_x_species,
  traits         = species_trait_df,   # rewrites to loadings_prior$features
  phylo          = phylo_tree,         # cophenetic to loadings_prior$distances
  family         = bernoulli(),         # default; binomial / poisson / nmix / occ also valid
  n_lv           = ...,
  ...
)
```

**Touch points:**
- New `R/jsdgam.R`:
  - Validate the response shape (sites by species, long-format
    convention).
  - Validate `traits` against the species set (rows match
    species levels, no all-NA columns).
  - If `phylo` is supplied: `ape::is.ultrametric` check, run
    `ape::cophenetic.phylo`, build the pairwise distance matrix,
    auto-standardise (`max(d) = 1`).
  - Rewrite call to `mvgam(..., loadings_prior = list(features =
    encoded_traits, distances = list(phylo = phylo_dist)))`.
  - Return value: class `c("jsdgam", "mvgam")` so existing S3
    methods dispatch normally; specific `print.jsdgam` and
    `summary.jsdgam` add ecology-flavoured framing.
- `R/how_to_cite.R::reference_db()` — add Warton et al. 2015,
  Ovaskainen et al. 2017, and Wilkinson et al. 2019 for JSDM
  references. Detection predicate `uses_jsdgam()` so a `jsdgam`
  fit automatically pulls these.
- `vignettes/online/jsdgam-informed-loadings.Rmd` — currently a
  promoted draft. Becomes the worked example for this surface.
- `R/ordinate.R` — `ordinate.jsdgam` already exists from #161;
  verify nothing breaks with the wrapper class.

**Tests:**
- `tests/testthat/test-jsdgam.R` — argument validation, alias
  rewriting, class dispatch, ecology-specific summary content.
- Local end-to-end recovery fixture in
  `tests/local/test-jsdgam-fit.R` simulating from a known trait
  + phylogeny structure and recovering the structured loadings.

**Effort:** 1.5 to 2.5 days. Most of the time is validation and
the ecology-flavoured summary / print methods; the alias rewrite
is small.

---

## 6. Vignette + roxygen example overhaul

**Source:** Tasks #106 (example audit), #107 (docs + commit),
#108 (vignette audit). The audit doc at `tasks/vignette_audit.md`
is the pre-overhaul characterisation; rewriting has not started.

**Goal.** Vignettes and roxygen examples still reference master-
era APIs (`type = "trend"` in `predict()`, `mvgam_predict`,
`compare_mvgams`, `eval_mvgam`, removed plotting wrappers, etc.).
The audit doc lists drift per vignette.

**Per-vignette work:**
- `data_in_mvgam.Rmd`: drop `trend_model = 'None'` references;
  the default is now a constructor.
- Sweep all `vignettes/*.Rmd` for `mvgam_predict`, `compare_mvgams`,
  `eval_mvgam`, `roll_eval_mvgam`, `lv_correlations`,
  `plot_mvgam_*`. Replace with current surfaces (`posterior_*`,
  `loo_compare`, `residual_cor`, `plot.mvgam`).
- Roxygen examples: any `@examples` block still calling removed
  symbols. Use the `mvgam_examples_skip()` guard for cmdstanr-
  gated chunks.

**New online-only vignette: brms observation specials in mvgam
state-space models.** Path:
`vignettes/online/brms-specials-in-ss.Rmd`. Walks the user through
each supported brms observation-level special with a small worked
example per case:

- `cens()` — interval-censored counts on a Poisson series with
  trend_formula = `RW()`; show how the censored likelihood
  composes with the latent dynamic process.
- `trials()` — binomial with varying trial counts (e.g. detection
  surveys with variable effort); `AR(p = 1)` latent trend on logit
  scale.
- `se()` — known sampling error on a Gaussian response (e.g.
  measurement-error-aware mean), gaussian family with `sigma =
  TRUE`, `RW(cor = TRUE)` latent process for related series.
- `me()` — measurement error on a continuous predictor in a state-
  space context; show recovery of the underlying coefficient when
  noise variance is correctly specified.
- `cs()` — category-specific covariate effects in a cumulative
  family with latent trends per ordinal threshold.
- `mm()` — multiple-membership grouping when sites belong to
  multiple ecoregions; varying intercepts via `mm()` plus an
  `AR()` latent trend.
- `car()` — spatial conditional autoregression on site means with
  a temporal `AR()` trend layered on top; demonstrate the `data2`
  adjacency-matrix workflow end-to-end.

Bundle with cached fits under `vignettes/online/_cache/` so CI
runtimes stay bounded. Cross-link from `?mvgam` and from the
`tests/testthat/test-brms-observation-specials.R` header so users
who hit the test as a contract know where to find the long-form
explanation.

**Tests:** `R CMD build && R CMD check --as-cran` clean on the
rebuilt vignettes. Vignettes that fit models should use cached
output under `vignettes/online/` to keep CI under control.

**Effort:** 3 to 5 days. Most of the time is rewriting prose and
re-running fits for cached output.

---

## Deferred below the cut

### lfo_cv prior pinning (#103 / Task #188) — already correct; docs sharpened

**Resolved 2026-06-09.** Empirical timing on lfo_cv showed the
cache hits already work as intended: 35 refits in 67.9 s
(~1.9 s per refit; cold-compile baseline is ~50 s). Root cause
of the original concern was not a code defect but a stale
mental model. The actual mechanism:

- `mvgam_update_inheritance` (see `R/update.mvgam.R`) inherits
  `prior` from `object$prior` on every `update()` call.
- `object$prior` stores the LITERAL prior table mvgam handed
  to brms at first fit — including the adaptive Intercept /
  sigma literals (e.g. `student_t(3, 50.6, 4.1)` for Gaussian,
  `student_t(3, 1.9, 2.5)` for Poisson, `inv_gamma(0.4, 0.3)`
  for NB shape).
- Because brms sees an explicit prior for the Intercept and
  family aux on the new `update()` call, it skips the adaptive
  regeneration and the stancode is byte-identical → compiled
  Stan model is reused. Confirmed for Gaussian, Poisson, and
  Negative Binomial.

Shipped under this task:

- `update.mvgam` roxygen extended with a "Prior inheritance
  and Stan model reuse" section that names the invariant and
  shows the override pattern (`update(fit, prior = ...)`).
- New example demonstrating prior override with `brms::prior()`.
- `get_prior.mvgam()` shortcut method added so users typing
  `get_prior(fit)` get the post-fit prior table directly
  (was previously only `prior_summary(fit)`).
- Contract test in `test-update.R` locks in the
  user-supplied-`prior` override path so any future
  inheritance refactor preserves it.

GH issue #103 can now be closed: there is no remaining work
item; the cache reuse was already correct, only the rationale
was missing from the user-facing docs.

### `methods_md(fit)` Markdown + LaTeX exporter (#187)

Stays parked. Per-trend and per-family equation registries need
to cover every supported family before this is worth shipping,
which means tasks 3 and 4 above must land first. Once they do,
the methods exporter becomes a roughly 0.5 to 1 day follow-up
sitting on top of the existing `how_to_cite()` reference
database and `bibtex()` S3 dispatch in `R/how_to_cite.R`.

### Variance decomposition + remaining brms helpers (Task #189)

From #109 ("More downstream functionality"). Two pieces, neither
blocks v2.0 ship:

- `linpred_variance_decomposition(fit)` returning per-term and
  trend share of linear-predictor variance.
- brms accessor parity (Task #189): `default_prior.mvgam`,
  `standata.mvgam` on the fitted object, and `make_stancode` /
  `make_standata` aliases. `prior_summary.mvgam`,
  `get_prior.mvgam_formula`, `stancode.mvgam`, and
  `standata.mvgam_formula` are already shipped. `get_inits` is
  not a brms generic — `rstan::get_inits(fit$fit)` already works.

### Other parked items

- `brm_multiple()` support — niche.
- `gratia`-class shim — depends on the variance decomposition
  surface and the helper passthroughs.
- Unified `build_trend_model()` dispatcher (#117) — internal
  refactor; defer to v2.1 if the trend registry grows beyond the
  current 7 constructors.
- `share_obs_params` deprecation — mechanical sweep; can ride
  with any other DESCRIPTION bump.
- Monotonic spline support via `smooth2random()` — open research
  question; not a v2.0 blocker.

### Items from #109 deferred for later consideration

- **Structural-process trends (BSTS-style).** Item 27 of #109:
  decompose the latent process into level, slope and seasonal
  components, each with its own (possibly multivariate) error
  term, composable with covariates in state-space form. New
  trend constructor family; meaningful Stan + R-side work. Park
  to v2.1.
- **`loadings_prior` extended beyond factor models.** Items 22
  and 23 of #109: route the same structured-prior machinery
  into `RW(cor = TRUE)` / `VAR(cor = TRUE)` / `ZMVN()` so users
  with phylogeny or trait side-information can shape correlated
  temporal dynamics, not just factor loadings. Heaps Section 4
  shows the framework already covers this case; the mvgam-side
  work is plumbing the spec through to the cor-trend Stan
  emission paths in `R/stan_assembly.R`.
- **Batch prior modification.** Item 19 of #109: sugar so users
  can change "all `gp()` length-scale priors at once" without
  enumerating each parameter. Small UX win.
- **brms `autocor` prediction path audit.** Item 30 of #109:
  verify that anything brms does for `autocor`-class terms
  (correlation in the residuals, AR1 / MA1 on the observation
  side) is either honoured or explicitly rejected by mvgam.
  Most of this is likely subsumed by `forecast.mvgam`; the
  audit confirms.
- **Stan parameter monitoring audit.** Item 17 of #109: confirm
  no oversized auxiliary parameter blocks are being saved when
  they could be post-computed. The architectural pattern is
  already in place; this is a sweep to find missed cases.
- **`summary(..., probs = ...)` printing for length > 2.** Item
  37 of #109: the `probs` argument already passes through to
  `quantile()`, but verify printing handles `length(probs) > 2`
  cleanly across `summary.mvgam`, `summary.mvgam_residcor`,
  `summary.mvgam_sim`, `summary.mvgam_forecast`,
  `summary.mvgam_lfo`. Small consistency pass.
