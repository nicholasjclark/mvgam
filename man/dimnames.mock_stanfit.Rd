% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mock-stanfit.R
\name{dimnames.mock_stanfit}
\alias{dimnames.mock_stanfit}
\title{Dimnames for a mock stanfit}
\usage{
\method{dimnames}{mock_stanfit}(x)
}
\arguments{
\item{x}{A mock_stanfit object created by \code{create_mock_stanfit()}.}
}
\value{
The dimnames of the underlying \code{draws_matrix} (a list
with \code{NULL} rows and the parameter names as columns).
}
\description{
Defers to the cached draws-matrix so callers that consult
\code{colnames(prep$draws)} / \code{dim(prep$draws)} (for example
\code{detect_gp_terms()} and other GP / smooth / RE detection
helpers in \code{R/predictions.R}) work against a mock_stanfit
without falling through to the default \code{dimnames.stanfit}
method, which reads \verb{@mode} and has no implementation here.
}
