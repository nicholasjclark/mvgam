% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/var_param_extractors.R
\name{posterior_transition_matrix}
\alias{posterior_transition_matrix}
\title{Posterior transition matrix for a VAR trend}
\usage{
posterior_transition_matrix(
  object,
  groups = NULL,
  summary = TRUE,
  robust = FALSE,
  probs = c(0.025, 0.975)
)
}
\arguments{
\item{object}{A fitted \code{mvgam} object whose trend is a
VAR(1).}

\item{groups}{Which panels of a hierarchical VAR fit to
return. \code{NULL} (the default) gives the global shrinkage
target built from \code{Amu_trend}, and is the only setting a
non-hierarchical fit accepts. Name one panel, by level name
or by integer index into the fitted grouping factor's levels
as stored on \code{object$data[[gr]]}, and that panel's matrix is
returned on its own. Name several, or pass \code{"all"}, and the
result is a named list of \code{mvgam_var_matrix} objects classed
as \code{mvgam_var_matrix_list}, so \code{plot()} produces a faceted
panel (heatmap grid or per-outcome self-persistence
pointrange, see \code{\link[=plot.mvgam_var_matrix_list]{plot.mvgam_var_matrix_list()}}).}

\item{summary}{Logical. \code{TRUE} (default) returns an
\code{mvgam_var_matrix} object with per-cell point estimates,
credible intervals, standard errors, effective sample
sizes and sign probabilities. \code{FALSE} returns the raw
\verb{[ndraws, K, K]} posterior array with dims 2-3 labelled by
the outcome names (levels of \code{subgr} on a hierarchical fit,
\code{series_names} on a single-panel fit) for downstream custom
summaries.}

\item{robust}{Logical. When \code{summary = TRUE}, use the median
as the per-cell point estimate; defaults to the posterior
mean when \code{FALSE}.}

\item{probs}{Length-2 numeric vector of quantile probabilities
for the per-cell credible interval when \code{summary = TRUE}.
Defaults to \code{c(0.025, 0.975)}.}
}
\value{
One matrix, or a list of them when \code{groups} names more
than one panel. With \code{summary = TRUE}, an object of class
\code{mvgam_var_matrix} with slots \code{A}, \code{A_se}, \code{A_lower},
\code{A_upper}, \code{A_ess}, \code{prob_positive}, \code{prob_negative},
\code{prob_nonzero}, \code{n_series}, \code{series_names}, \code{group_label},
\code{probs}. Has \code{print()} and \code{plot()} methods. With
\code{summary = FALSE}, a \verb{[ndraws, K, K]} numeric array with
dims 2-3 labelled by the outcome names.
}
\description{
Extract the VAR(1) transition matrix $A$ from a fitted
\code{mvgam} object, either as a per-cell posterior summary
(default) or as raw per-draw values. Row $i$ of $A$ gives the
linear dependence of latent process $i$ at time $t$ on the
vector of processes at time $t - 1$; the diagonal is
self-persistence, off-diagonals encode cross-outcome dependence
at lag one.
}
\details{
The extractor dispatches on whether the fit is hierarchical
(\verb{VAR(gr = <var>, ...)}) or single-panel.
\itemize{
\item On a \strong{single-panel VAR} the routine reads the fitted
\code{A_trend} array.
\item On a \strong{hierarchical VAR} with \verb{group = <int>} or
\verb{group = <name>} it reads the panel-specific matrix
\code{A_group_trend[c, 1, , ]}, i.e. the transition matrix for
the requested country / patch / hospital.
\item On a \strong{hierarchical VAR} with \code{group = NULL} it synthesises
the shrinkage target from the hyperparameters:
diagonal cells take \code{Amu_trend[1, 1]}, off-diagonal cells
take \code{Amu_trend[2, 1]}. This is the mean the sampler pulls
every panel's $A_c$ toward, so it summarises the pool
without reading any single country
(Savage \href{https://rpubs.com/jimsavage/hierarchical_var}{2016}).
}

With \code{summary = TRUE} (default) the per-cell summary uses the
same machinery as \code{residual_cor()}, so the printed and
plotted output stays consistent across the two extractors:
point estimates default to the posterior mean; intervals use
\code{probs} on the raw draws; effective sample sizes come from
\code{posterior::ess_basic()}; and
\code{prob_positive} / \code{prob_negative} / \code{prob_nonzero} count the
fraction of draws with each entry above or below zero. See
the argument documentation for the median switch.

Only VAR(1) fits are supported; the \code{A_trend} slot has a
single lag dimension pinned to one for compatibility with
mvgam's \code{irf()}, \code{fevd()} and \code{stability()} methods.
}
\references{
Savage J (2016). Hierarchical Vector
Autoregression. RPubs, 27 November 2016.
\url{https://rpubs.com/jimsavage/hierarchical_var}

Heaps SE (2023). Enforcing stationarity through the prior in
vector autoregressions.
\emph{Journal of Computational and Graphical Statistics}
32(1), 74-83.
\doi{10.1080/10618600.2022.2079648}
}
\seealso{
\code{\link[=VAR]{VAR()}}, \code{\link[=residual_cor]{residual_cor()}}, \code{\link[=irf]{irf()}}, \code{\link[=fevd]{fevd()}},
\code{\link[=stability]{stability()}}, \code{\link[=print.mvgam_var_matrix]{print.mvgam_var_matrix()}},
\code{\link[=plot.mvgam_var_matrix]{plot.mvgam_var_matrix()}}.
}
\author{
Nicholas J Clark
}
