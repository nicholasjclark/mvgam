% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_helpers.R
\name{pivot_species_matrix}
\alias{pivot_species_matrix}
\title{Pivot a wide species matrix to mvgam long-format}
\usage{
pivot_species_matrix(
  Y,
  site_covs = NULL,
  traits = NULL,
  site_id_name = "time",
  series_name = "series",
  values_to = "y"
)
}
\arguments{
\item{Y}{A \verb{[J_sites, N_species]} matrix or \code{data.frame}. Column
names become the \code{series} factor levels; missing column names
default to \verb{sp1, sp2, ...}.}

\item{site_covs}{Optional \code{data.frame} of site-level covariates
with \code{nrow == J_sites}. The \code{Hmsc} \code{XData} slot maps here.}

\item{traits}{Optional \code{data.frame} of per-species traits with
\code{nrow == N_species}. Broadcast across sites. The \code{Hmsc}
\code{TrData} slot maps here.}

\item{site_id_name}{Name of the site identifier in the output.
Defaults to \code{"time"}.}

\item{series_name}{Name of the species column. Defaults to
\code{"series"}.}

\item{values_to}{Name of the response column. Defaults to \code{"y"}.}
}
\value{
A long-format \code{data.frame} with \code{J * N} rows, each
carrying \code{series}, \code{time}, \code{y}, site covariates broadcast
across species, and species traits broadcast across sites.
}
\description{
Convert the wide \verb{[J sites, N species]} response matrix used by
\code{Hmsc} (and any single-visit JSDM workflow) into the long-format
\verb{(series, time, y, ...)} layout that \code{mvgam()} / \code{jsdgam()} with
a simplex (\code{diri()}, \code{multi()}, \code{categ()}), continuous-response
(\code{mvn()}, \code{mvt()}), or bernoulli / binomial / poisson family
expects.
}
\author{
Nicholas J Clark
}
