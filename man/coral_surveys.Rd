% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coral_surveys.R
\docType{data}
\name{coral_surveys}
\alias{coral_surveys}
\title{Simulated coral bleaching index with irregular CAR(1) state}
\format{
A \code{data.frame} with 150 rows and the following fields:
\describe{
\item{series}{factor identifying the reef (\code{flynn},
\code{myrmidon}, \code{pixie}); duplicated as \code{reef}.}
\item{time}{integer month index (irregularly spaced within
each reef).}
\item{y}{numeric bleaching index (Gaussian, scaled);
duplicated as \code{bleaching}.}
\item{reef}{factor, alias of \code{series}.}
\item{sst}{numeric observed sea-surface temperature in
degrees Celsius.}
\item{bleaching}{numeric bleaching index, alias of \code{y}.}
}
}
\usage{
coral_surveys
}
\description{
A state-space dataset for three coral reefs surveyed at
irregular monthly intervals (gap distribution
\code{Uniform(1, 6)} months, mean approximately 3.5). The latent
state is a continuous-time AR(1) (CAR(1)) trend per reef with
\code{phi = 0.7} and \code{sigma = 0.5}. The observation equation adds
an observed sea-surface temperature (SST) driver via a smooth
nonlinear effect.
}
\details{
Naive AR(1) treats every consecutive observation as a unit gap
and biases \code{phi} downward toward \code{phi^mean(dt)} (roughly 0.29
for this dataset), leaving the SST smooth to absorb persistence
that belongs to the latent state. \code{trend_formula = ~ CAR()}
with the supplied \code{time} column recovers \code{phi} near \code{0.7} in
continuous time and renders the SST smooth identifiable.

True generative components:
\itemize{
\item Per-reef intercepts (centred).
\item Latent CAR(1) state per reef with \code{phi = 0.7} and
\code{sigma = 0.5}, propagated through
\code{trend[t] = phi^dt * trend[t-1] + N(0, sigma * sqrt((1 - phi^(2*dt)) / (1 - phi^2)))}.
\item Shared smooth nonlinear obs-side effect of SST, evaluated via
a thin-plate basis.
\item Observation noise \code{sigma_obs = 0.3}.
}

Recommended fit (state-space; SST on the observation equation,
CAR(1) on the latent state):

\if{html}{\out{<div class="sourceCode r">}}\preformatted{mvgam(
  formula = y ~ s(sst, k = 8),
  trend_formula = ~ CAR(),
  data = coral_surveys,
  family = gaussian()
)
}\if{html}{\out{</div>}}

Built by \code{data-raw/build_coral_surveys.R}.
}
\keyword{datasets}
