% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conditional_effects.mvgam.R
\name{conditional_effects.mvgam}
\alias{conditional_effects.mvgam}
\alias{plot.mvgam_conditional_effects}
\alias{print.mvgam_conditional_effects}
\title{Display conditional effects of predictors for mvgam models}
\usage{
\method{conditional_effects}{mvgam}(
  x,
  effects = NULL,
  type = "expected",
  points = FALSE,
  rug = FALSE,
  process_error = FALSE,
  ...
)

\method{plot}{mvgam_conditional_effects}(x, plot = TRUE, ask = FALSE, ...)

\method{print}{mvgam_conditional_effects}(x, ...)
}
\arguments{
\item{x}{An object of class \code{mvgam_conditional_effects}.}

\item{effects}{Optional character vector of effects to plot. If
\code{NULL} (the default), all main effects and interactions in the
formula are detected automatically.}

\item{type}{Scale of predictions. One of \code{"response"} (outcome
scale with observation-process noise), \code{"link"} (link-scale
linear predictor) or \code{"expected"} (E\link{Y}; default).}

\item{points}{Logical or numeric. If \code{TRUE} (or a non-zero alpha
between 0 and 1) and \code{type = "response"}, raw observations are
overlaid on the plot.}

\item{rug}{Logical. If \code{TRUE} and \code{type = "response"}, rug tick
marks are drawn along the axes.}

\item{process_error}{Logical. Passed to \code{plot_predictions()} /
\code{get_predict.mvgam}. Defaults to \code{FALSE} so the latent trend
collapses to its posterior mean; set \code{TRUE} to include per-draw
latent-state uncertainty.}

\item{...}{Ignored.}

\item{plot}{Logical. If \code{TRUE} (default), draws each plot;
otherwise returns the list invisibly so callers can post-process.}

\item{ask}{Logical. If \code{TRUE}, prompts before each new plot.}
}
\value{
An object of class \code{mvgam_conditional_effects}: a named
list with one \code{ggplot} per effect, drawn by the \code{plot} and
\code{print} methods.

Invisibly returns the list of ggplot objects.
}
\description{
Plot the conditional effects of one or more numeric / categorical
predictors in fitted \code{mvgam} models, including up-to-three-way
interactions. A thin wrapper around
\code{\link[marginaleffects:plot_predictions]{marginaleffects::plot_predictions()}} that auto-enumerates the
model's term labels (across both observation and trend submodels)
and produces one ggplot per term.
}
\seealso{
\code{\link[marginaleffects:plot_predictions]{marginaleffects::plot_predictions()}},
\code{\link[marginaleffects:plot_slopes]{marginaleffects::plot_slopes()}}
}
\author{
Nicholas J Clark
}
