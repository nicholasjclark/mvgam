% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fitted.R
\name{fitted.mvgam}
\alias{fitted.mvgam}
\title{Expected Values from the Posterior Distribution}
\usage{
\method{fitted}{mvgam}(
  object,
  newdata = NULL,
  re_formula = NULL,
  scale = c("response", "linear"),
  components = c("response", "latent_state", "detection"),
  unit_level = NULL,
  resp = NULL,
  ndraws = NULL,
  summary = TRUE,
  robust = FALSE,
  probs = c(0.025, 0.975),
  process_error = TRUE,
  allow_new_levels = FALSE,
  sample_new_levels = "uncertainty",
  ...
)
}
\arguments{
\item{object}{An object of class \code{mvgam}.}

\item{newdata}{An optional data.frame of new predictor values. If
\code{NULL} (the default), the training data stored in \code{object} is
used. This is what makes the call "fitted" rather than a general
prediction.}

\item{re_formula}{Formula for random effects. If \code{NULL} (the
default), all random effects are included. Use \code{NA} to exclude
all random effects.}

\item{scale}{Character; one of \code{"response"} (default) or \code{"linear"}.
\code{"response"} returns expected values on the response scale via
\code{\link[=posterior_epred.mvgam]{posterior_epred.mvgam()}}. \code{"linear"} returns the linear predictor
on the link scale via \code{\link[=posterior_linpred.mvgam]{posterior_linpred.mvgam()}}. Ignored when
\code{components} is not \code{"response"}.}

\item{components}{Character; one of \code{"response"} (default),
\code{"latent_state"}, or \code{"detection"}. Selects which posterior
component to return.
\itemize{
\item \code{"response"} keeps the brms-default behaviour: marginal
expected values at the per-row grain, scaled via \code{scale}.
\item \code{"latent_state"} (closure-unit families only) routes through
\code{\link[=predict.mvgam]{predict.mvgam()}} with \code{type = "latent_state"}, returning the
conditional latent state per closure unit (\code{P(z = 1 | y)} for
\code{occ()}, posterior \code{N} for \code{nmix()} variants).
\item \code{"detection"} (closure-unit families only) routes through
\code{\link[=predict.mvgam]{predict.mvgam()}} with \code{type = "detection"}, returning per-
visit detection probabilities.
}

Modelled on the \code{flocker::fitted_flocker(components = ...)}
argument so flocker users have a one-call equivalent.}

\item{unit_level}{Logical or \code{NULL} (default). When \code{TRUE} for a
closure-unit detection family and \code{components = "response"},
aggregates the per-visit response matrix to the per-unit grain
(sum across visits within a unit) via the shared
\code{aggregate_closure_unit_visits()} helper. Ignored when
\code{components != "response"} (those grains are fixed by the
component definition) and when the family is not a
visit-aggregating closure-unit family.}

\item{resp}{Character specifying which response variable for
multivariate models. If \code{NULL}, fitted values are returned for
all responses (a named list).}

\item{ndraws}{Positive integer specifying the number of posterior
draws to use. If \code{NULL} (the default), all draws are used.}

\item{summary}{Logical. If \code{TRUE} (the default), returns summary
statistics. If \code{FALSE}, returns the full matrix of posterior
draws.}

\item{robust}{Logical. If \code{FALSE} (the default), uses mean and
standard deviation for summaries. If \code{TRUE}, uses median and
median absolute deviation (MAD).}

\item{probs}{Numeric vector of probabilities for quantile
computation. Default is \code{c(0.025, 0.975)} for 95\% credible
intervals.}

\item{process_error}{Logical. If \code{TRUE} (the default), uses the
full posterior draws of trend parameters. If \code{FALSE}, fixes the
trend at its posterior mean for faster computation. Stochastic
process innovations are not added here (only by
\code{\link[=posterior_predict.mvgam]{posterior_predict.mvgam()}}); fitted values stay deterministic
functions of the parameter draws.}

\item{allow_new_levels}{Logical. If \code{TRUE}, allows predictions for
new factor levels not seen during training. Default is \code{FALSE}.}

\item{sample_new_levels}{Character specifying how to sample new
levels. Either \code{"uncertainty"} (default), \code{"gaussian"}, or
\code{"old_levels"}.}

\item{...}{Additional arguments passed to the underlying posterior
methods.}
}
\value{
If \code{summary = FALSE}, returns a matrix of posterior draws
with dimensions \verb{[ndraws x nobs]}.

If \code{summary = TRUE}, returns a matrix with columns:
\itemize{
\item \code{Estimate}: Point estimate (mean or median depending on
\code{robust})
\item \code{Est.Error}: Uncertainty estimate (sd or mad depending on
\code{robust})
\item \verb{Q*}: Quantile columns corresponding to values in \code{probs}
}

For multivariate models with \code{resp = NULL}, returns a named list
of matrices (one per response variable), each summarised
independently when \code{summary = TRUE}.
}
\description{
Compute fitted values for an mvgam model, returning the expected
value E[Y | X] (or the linear predictor, with \code{scale = "linear"})
at the training observations by default. Mirrors the brms
\code{\link[brms:fitted.brmsfit]{fitted.brmsfit()}} interface as closely as
the underlying mvgam posterior methods permit.
}
\details{
\code{fitted.mvgam()} follows the brms convention: it returns the
expected value E[Y | X] (or the linear predictor with \code{scale = "linear"}) and does \strong{not} include observation-level noise or the
state-space process noise that \code{\link[=posterior_predict.mvgam]{posterior_predict.mvgam()}} adds.
Use \code{predict()} if you want predictive samples that include those
noise components.

Two brms \code{fitted()} arguments are not yet supported and will be
ignored if passed via \code{...}:
\itemize{
\item \code{dpar} / \code{nlpar}: distributional and non-linear parameter
selection (waiting on broader distributional regression
support in mvgam).
\item \code{draw_ids} / \code{sort}: draw subsetting by index and series
sorting are not yet plumbed through the underlying mvgam
posterior methods. Use \code{ndraws} for subsetting.
}
}
\examples{
\donttest{
set.seed(13)
simdat <- sim_mvgam(family = poisson(), n_series = 1L,
                     n_timepoints = 120L, trend_model = AR())
mod <- mvgam(y ~ s(x), trend_formula = ~ AR(p = 1),
              data    = simdat$data_train,
              family  = poisson(),
              chains  = 2, silent = 2)

# Summary matrix of fitted values on the response scale.
ft <- fitted(mod)
head(ft)

# Per-cell residuals on the response scale (observed minus
# posterior mean). Use `pp_check(mod, type = "resid_ribbon")`
# for a graphical view that respects the model family.
resids <- simdat$data_train$y - ft[, "Estimate"]
summary(resids)
}

}
\seealso{
\code{\link[=posterior_epred.mvgam]{posterior_epred.mvgam()}}, \code{\link[=posterior_linpred.mvgam]{posterior_linpred.mvgam()}},
\code{\link[=posterior_predict.mvgam]{posterior_predict.mvgam()}}, \code{\link[=predict.mvgam]{predict.mvgam()}},
\code{\link[brms:fitted.brmsfit]{brms::fitted.brmsfit()}}.
}
