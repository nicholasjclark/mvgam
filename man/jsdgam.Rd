% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jsdgam.R
\name{jsdgam}
\alias{jsdgam}
\title{Fit Joint Species Distribution Models in mvgam}
\usage{
jsdgam(
  formula,
  factor_formula = ~-1,
  knots,
  factor_knots,
  data,
  newdata,
  family = binomial(),
  unit = time,
  species = series,
  share_obs_params = FALSE,
  priors,
  n_lv = 2L,
  backend = getOption("brms.backend", "cmdstanr"),
  ...
)
}
\arguments{
\item{formula}{A \code{formula} object specifying the GAM observation
model formula. These behave exactly like the formula for a GLM
except that smooth terms (\code{s()}, \code{te()}, \code{ti()}, \code{t2()}), time
varying \code{dynamic()} terms, nonparametric \code{gp()} terms and
\code{offset()} can be added to the right-hand side. See
\code{mvgam_formulae} for details.}

\item{factor_formula}{A \code{formula} for the latent factors' linear
predictor. Use \code{by = lv_axis()} inside \code{s()}, \code{te()}, \code{ti()},
\code{t2()}, or \code{gp()} terms to fit a separate smooth per latent
factor. The legacy spelling \code{by = trend} is still accepted and
emits a one-time deprecation warning before being rewritten to
\code{by = lv_axis()} internally. The companion sentinel \code{lv_axis()}
is documented at \verb{[lv_axis()]}.}

\item{factor_knots}{An optional \code{list} of knot values for any
smooth terms in \code{factor_formula}, mirroring the role of
\code{knots} for the observation formula.}

\item{data}{A \code{data.frame} or \code{list} containing the response
variable and the covariates referenced by \code{formula} and
\code{factor_formula}. Must contain the column named by \code{unit} (the
sampling-unit index, often \code{time} or \code{site}) and the factor
column named by \code{species} (the response-unit index).}

\item{newdata}{Optional test-set \code{data.frame} persisted on the
fit as \code{object$test_data}. Used by \code{plot(fit, type = "series")}
to overlay the test arm without re-passing the data. Not fed
to Stan at fit time — supply it to \code{posterior_predict()} /
\code{forecast()} for out-of-sample evaluation.}

\item{family}{A \code{family} object specifying the observation
distribution. Supported families are documented in
\code{mvgam_families}. Defaults to \code{binomial()}, which is the standard
choice for presence/absence JSDM responses; switch to a count
family (\code{poisson()}, \code{nb()}) when modelling counts.}

\item{unit}{The unquoted name of the \code{numeric/integer} variable
that indexes the sampling unit (typical names: \code{time} or
\code{site}). Defaults to \code{time}.}

\item{species}{The unquoted name of the \code{factor} variable that
indexes the different response units (typical name in a JSDM:
\code{species}). Defaults to \code{series} to stay consistent with other
mvgam models.}

\item{share_obs_params}{Logical. Forwarded to \code{mvgam}.}

\item{priors}{Optional \code{data.frame} or \code{brmsprior} vector with
prior overrides. See \verb{[get_mvgam_priors]} and \verb{[brms::prior()]}
for the conventions.}

\item{n_lv}{\code{integer}. Number of latent factors to use for
modelling residual associations. Must be \verb{>= 1} and strictly
less than the number of species. Defaults to \code{2}.}

\item{backend}{Stan backend (defaults to "cmdstanr")}

\item{...}{Other arguments forwarded to \code{mvgam()}. Notable ones
include \code{loadings_prior} (for trait- and phylogeny-informed
priors on Z), \code{algorithm}, \code{chains}, \code{silent}, \code{run_model}.}
}
\value{
A \code{list} of class \code{c("mvgam", "jsdgam")}. The full mvgam
method surface (\code{summary}, \code{predict}, \code{forecast}, \code{loo},
\code{posterior_epred}, etc.) applies; jsdgam-specific surfaces
(\code{ordinate}, \code{residual_cor}) recognise the additional class.
}
\description{
Sets up a Joint Species Distribution Model (JSDM) in which the
residual associations among species are modelled in a reduced-rank
format using a set of latent factors. The factor specification is
flexible: spatial, temporal, or any other type of predictor effects
can enter the latent factors via \code{factor_formula}, while the
observation model itself supports all the smooth, GP and random
effects that mvgam can handle. Use \code{by = lv_axis()} inside smooth
or GP terms of \code{factor_formula} to fit per-latent-factor smooths
(constrained ordination).
}
\references{
Warton, D. I., Blanchet, F. G., O'Hara, R. B., Ovaskainen, O.,
Taskinen, S., Walker, S. C. and Hui, F. K. C. (2015). So many
variables: joint modeling in community ecology. \emph{Trends in
Ecology and Evolution}, 30(12):766-779. \doi{10.1016/j.tree.2015.09.007}

Heaps, S. E. and Jermyn, I. H. (2024). Structured prior
distributions for the covariance matrix in latent factor
models. \emph{Statistics and Computing}, 34:143.
\doi{10.1007/s11222-024-10454-0}
}
\seealso{
\verb{[mvgam()]}, \verb{[lv_axis()]}, \verb{[residual_cor()]},
\verb{[ordinate()]}.
}
