% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare_loadings.R
\name{compare_loadings}
\alias{compare_loadings}
\title{Compare posterior loadings between two latent-factor fits}
\usage{
compare_loadings(
  fit_a,
  fit_b,
  labels = c("fit_a", "fit_b"),
  which_lvs = c(1L, 2L),
  align = TRUE,
  facet = FALSE,
  label_species = c("all", "shift", "none"),
  ...
)
}
\arguments{
\item{fit_a, fit_b}{Two fitted \code{mvgam} / \code{jsdgam} objects, each
with \code{n_lv >= 2} and the same species set in the same factor
order.}

\item{labels}{Optional length-2 character vector of fit labels
shown in the legend / facet strips. Defaults to
\code{c("fit_a", "fit_b")}.}

\item{which_lvs}{Integer vector of length 2 indicating which
two latent factors to plot. Defaults to \code{c(1, 2)}.}

\item{align}{Logical. When \code{TRUE} (default) \code{fit_b}'s
posterior-median \code{Z_tilde} is rotated onto \code{fit_a}'s basis via
orthogonal Procrustes before plotting. Set to \code{FALSE} to keep
both fits in their own QR-identified basis (rarely useful
except for sanity checks on rotation invariance).}

\item{facet}{Logical. When \code{FALSE} (default) both fits are
overlaid on the same axes in different colours, with
segments connecting the same species across fits. When
\code{TRUE}, fits are drawn as side-by-side facets on shared axes.}

\item{label_species}{Character. Controls which species are
labelled. \code{"all"} (default) labels every species; \code{"shift"}
labels the top 10 species by post-alignment displacement
between fits; \code{"none"} draws unlabelled points only.}

\item{...}{Ignored.}
}
\value{
A \code{ggplot} object.
}
\description{
Side-by-side comparison of the posterior-median species loadings
(\code{Z_tilde}) from two latent-factor \code{mvgam} / \code{jsdgam} fits on
the same set of species. The two posteriors are identified only
up to rotation of the factor basis, so a direct overlay would
be misleading without an alignment step. \code{compare_loadings()}
applies the orthogonal Procrustes solution (Schoenemann 1966)
that rotates \code{fit_b}'s posterior-median \code{Z_tilde} to best match
\code{fit_a}'s in least-squares sense, then either overlays both
onto a single biplot with displacement segments (default) or
splits them into two facets sharing axes.

Useful when teaching the effect of a structured loadings prior:
fit one model with \code{loadings_prior = NULL} and a second with
\verb{traits =} or \verb{phylo =}, then plot the comparison to show how
the trait or phylogeny similarity reshapes the latent positions.
}
\details{
The orthogonal Procrustes rotation is the closed-form solution
\deqn{R = U V^T} where \eqn{U \Sigma V^T = SVD(Z_a^T Z_b)} and
\eqn{Z_a, Z_b} are the posterior-median loading matrices
restricted to \code{which_lvs}. The aligned \code{Z_b} is \code{Z_b \%*\% t(R)}.
Alignment is a sufficient but not unique solution to factor
rotation indeterminacy; alternative alignments (varimax on the
joint matrix, sign-flip only) are possible but typically less
interpretable for the prior-comparison use case here.

Both fits must report the same species in the same factor
order; the function errors otherwise. To compare fits with
different species sets, subset before fitting.
}
\references{
Schoenemann PH (1966). A generalized solution of the orthogonal
Procrustes problem. \emph{Psychometrika} 31, 1-10.
\doi{10.1007/BF02289451}

Heaps SE and Jermyn IH (2024). Structured prior distributions
for the covariance matrix in latent factor models. \emph{Statistics
and Computing} 34, 143. \doi{10.1007/s11222-024-10454-0}
}
\seealso{
\code{\link[=ordinate.mvgam]{ordinate.mvgam()}}, \code{\link[=ordinate.jsdgam]{ordinate.jsdgam()}},
\code{\link[=residual_cor]{residual_cor()}}, \code{\link[=active_factors]{active_factors()}}
}
\author{
Nicholas J Clark
}
