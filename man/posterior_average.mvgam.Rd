% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pp_average.mvgam.R
\name{posterior_average.mvgam}
\alias{posterior_average.mvgam}
\title{Model-averaged posterior draws across \pkg{mvgam} fits}
\usage{
\method{posterior_average}{mvgam}(
  x,
  ...,
  variable = NULL,
  pars = NULL,
  weights = "stacking",
  ndraws = NULL,
  nsamples = NULL,
  missing = NULL,
  model_names = NULL,
  control = list(),
  seed = NULL
)
}
\arguments{
\item{x}{A fitted \code{mvgam} object. Further fits are passed via
\code{...}.}

\item{...}{Additional fitted \code{mvgam} objects to average over,
and (named) additional arguments passed through to
\code{\link[=as.data.frame.mvgam]{as.data.frame.mvgam()}} / \code{\link[=variables.mvgam]{variables.mvgam()}}.}

\item{variable}{Optional character vector of parameter names
to extract. \code{NULL} (the default) selects all parameters
shared across fits (or all parameters across the union when
\code{missing} is supplied), excluding \code{"lp__"}.}

\item{pars}{Deprecated brms alias for \code{variable}.}

\item{weights}{Either a numeric vector with one entry per fit
(auto-normalised to sum to 1) or one of \code{"stacking"},
\code{"pseudobma"}, \code{"pseudobma+"}, \code{"loo"}, \code{"waic"}. See
\code{mvgam_loo_extras} for the per-strategy semantics.}

\item{ndraws}{Total number of model-averaged draws to return.
Defaults to the number of draws in the first fit.}

\item{nsamples}{Deprecated brms alias for \code{ndraws}.}

\item{missing}{Default values for parameters that are absent
from some of the supplied fits. \code{NULL} (the default)
restricts \code{variable} to the intersection across fits and
errors if any requested parameter is not in every fit. A
list keyed by parameter name supplies per-parameter
defaults; a single numeric is broadcast across every
missing parameter in every fit that lacks it.}

\item{model_names}{Optional character vector naming the fits
in the same order they were passed.}

\item{control}{A list of extra arguments passed to
\code{\link[loo:loo_model_weights]{loo::loo_model_weights()}} when computing stacking /
pseudo-BMA weights.}

\item{seed}{Optional integer seed for reproducibility.}
}
\value{
A \code{data.frame} of model-averaged posterior draws.
Attributes \code{weights} and \code{ndraws} record the per-model
weight and the integer draw count drawn from each fit.
}
\description{
Returns Bayesian model-averaged samples from the posterior of
two or more \code{mvgam} fits, in proportion to the supplied (or
computed) model weights. Mirrors
\code{\link[brms:posterior_average.brmsfit]{brms::posterior_average.brmsfit()}}.
}
\examples{
\dontrun{
set.seed(13)
simdat <- sim_mvgam(family = poisson(), n_series = 1L,
                     n_timepoints = 120L, trend_model = AR())
mod1 <- mvgam(y ~ s(x), trend_formula = ~ AR(p = 1),
               data    = simdat$data_train, family = poisson(),
               chains  = 2, silent = 2)
mod2 <- mvgam(y ~ 1,    trend_formula = ~ AR(p = 1),
               data    = simdat$data_train, family = poisson(),
               chains  = 2, silent = 2)

# 50:50 average of one trend parameter across the two fits.
pa <- posterior_average(mod1, mod2, variable = "ar1_trend[1]",
                         weights = c(0.5, 0.5))
summary(pa)
}

}
\seealso{
\code{\link[brms:posterior_average.brmsfit]{brms::posterior_average.brmsfit()}},
\code{\link[=pp_average.mvgam]{pp_average.mvgam()}}, \link{mvgam_loo_extras}.
}
\author{
Nicholas J Clark
}
