% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ordinate.jsdgam.R
\name{ordinate.jsdgam}
\alias{ordinate.jsdgam}
\alias{ordinate}
\title{Latent variable ordination plots from \code{jsdgam} objects}
\usage{
ordinate(object, ...)

\method{ordinate}{jsdgam}(
  object,
  which_lvs = c(1L, 2L),
  biplot = TRUE,
  alpha = 0.5,
  rotation = c("svd", "varimax", "promax", "none"),
  label_sites = TRUE,
  traits = NULL,
  trait_arrow_scale = 1,
  ...
)
}
\arguments{
\item{object}{A fitted \code{jsdgam} object.}

\item{...}{Ignored.}

\item{which_lvs}{Integer vector of length 2 indicating the
two re-rotated latent variables to plot. Defaults to
\code{c(1, 2)}.}

\item{biplot}{Logical. When \code{TRUE} (the default) both site
scores and species loading arrows are drawn; when \code{FALSE},
site scores only.}

\item{alpha}{Proportional numeric scalar in \verb{[0, 1]}
controlling the relative scaling of latent variables vs
loading coefficients.}

\item{rotation}{Character. Post-hoc rotation of the
posterior-median LV trends and Z loadings before plotting.
One of:
\describe{
\item{\code{"svd"} (default)}{BORAL convention: SVD of
\code{LV \%*\% t(Z)} re-orders axes by singular-value variance.
Use \code{alpha} to split the variance between site scores and
loadings.}
\item{\code{"varimax"}}{Orthogonal rotation maximising loading
sparsity. Each series tends to load strongly on one
factor and near-zero on others. \code{alpha} is ignored.
Requires \code{n_lv >= 2}.}
\item{\code{"promax"}}{Oblique extension of varimax (allows
correlated factors). \code{alpha} ignored.}
\item{\code{"none"}}{No rotation. Plot raw posterior-median LV
and Z, centred. \code{alpha} ignored.}
}
For varimax / promax the axes are NOT variance-ordered, so
the choice of \code{which_lvs} matters in a different way than
under SVD: any pair of rotated factors is a valid pair to
plot.}

\item{label_sites}{Logical. When \code{TRUE}, site scores are
drawn as labels (from the \code{unit} argument of the original
\code{jsdgam()} call); when \code{FALSE}, as points only.}

\item{traits}{Optional trait overlay. One of:
\describe{
\item{\code{NULL} (default)}{No trait arrows; the previous
biplot behaviour.}
\item{\code{"auto"}}{If the fit was trait-informed via
\code{jsdgam(traits = ...)}, pull the trait frame back off
the fit and use it as the overlay. Emits a one-time
warning and skips the overlay if no traits are found.}
\item{a \code{data.frame} or \code{matrix}}{Per-species trait values
(one row per species; columns are traits). Rows are
aligned via \code{rownames(traits)} when set; otherwise rows
must already match the fit's series-level order. Non-
numeric columns are ignored.}
}
When a non-NULL frame is in play, each numeric trait is
regressed on the rotated species loadings to give its
direction in the LV space; the resulting arrows are overlaid
on the biplot in steelblue. The regression-on-loadings recipe
works whether the fit was trait-informed via
\code{jsdgam(traits = ...)} or not, so it can either visualise the
structural trait gradient (informed) or serve as a post-hoc
overlay (naive). Only meaningful when \code{biplot = TRUE}.}

\item{trait_arrow_scale}{Positive numeric. Visual scaling
factor for trait-arrow lengths. The default \code{1} places the
longest trait arrow at the same radius as the longest species
arrow; values \verb{> 1} lengthen, values \verb{< 1} shorten.}
}
\value{
A \code{ggplot} object.
}
\description{
Plot an ordination of latent variables and their factor
loadings from a fitted \code{jsdgam} model. The two chosen latent
variables are first re-rotated via singular value
decomposition, then posterior medians of the variables and
the species' loadings are scattered in the resulting 2-D
space. Site labels and species loading arrows are drawn via
the package-wide \link[ggrepel:ggrepel]{ggrepel::ggrepel} helpers; when \pkg{ggarrow} and \pkg{ggpp}
are installed the species arrows are rendered as tapered
loadings.
}
\note{
\code{jsdgam()} itself is not yet ported on this branch;
this function exists for forward-compatibility with the
eventual jsdgam port. It relies on the same Stan parameters
that an LV \code{mvgam} fit carries — \code{lv_trend_tilde[t, k]} and
\code{Z_tilde[i, k]} for free-Z factor models, or \code{lv_trend} and
\code{Z} for partial-Z fits — plus a few \code{jsdgam}-specific
bookkeeping slots. Calling it on an ordinary \code{mvgam} object
will error at those slot accesses.
}
\examples{
\donttest{
set.seed(1)
simdat <- sim_closure_unit_data(
  family    = occ(),
  n_species = 4L,
  n_sites   = 50L,
  n_visits  = 4L,
  type      = 2L
)
mod <- jsdgam(
  formula        = bf(y ~ env, p ~ tod_c),
  factor_formula = ~ -1,
  data           = simdat$data_train,
  family         = occ(),
  n_lv           = 2L,
  chains         = 2,
  silent         = 2
)

# SVD biplot of site + species scores in the two-factor space.
ordinate(mod)
}

}
\seealso{
\code{\link[=jsdgam]{jsdgam()}}, \code{\link[=residual_cor]{residual_cor()}},
\code{\link[=plot.mvgam]{plot.mvgam()}} (especially \code{type = "factors"} and
\code{type = "latent_state"})
}
\author{
Nicholas J Clark
}
