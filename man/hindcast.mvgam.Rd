% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hindcast.mvgam.R
\name{hindcast.mvgam}
\alias{hindcast.mvgam}
\title{Posterior hindcasts for an mvgam state-space model}
\usage{
\method{hindcast}{mvgam}(
  object,
  ...,
  type = c("response", "link", "expected", "trend"),
  ndraws = NULL,
  obs_uncertainty = TRUE,
  resample_innovations = FALSE,
  resp = NULL
)
}
\arguments{
\item{object}{A fitted \link[=mvgam]{mvgam} object.}

\item{...}{Currently unused.}

\item{type}{One of \code{"response"}, \code{"link"}, \code{"expected"},
\code{"trend"}. \code{"response"} samples from the observation family
(the default); \code{"expected"} returns the family's mean
(\code{linkinv(eta)}); \code{"link"} returns the link-scale linpred;
\code{"trend"} returns the latent-trend trajectory on the link
scale.}

\item{ndraws}{Optional integer; the number of posterior draws
to use. Defaults to all available draws.}

\item{obs_uncertainty}{Logical. When \code{FALSE}, skips
observation-family sampling for \code{type = "response"},
returning the family mean (\code{linkinv(eta)}) instead.
Defaults to \code{TRUE}.}

\item{resample_innovations}{Logical. When \code{FALSE} (the
default), hindcasts use the Stan-fitted latent state directly
(\code{trend[t, s]} and \code{mu_trend[t, s]} read from the posterior);
\code{type = "response"} then samples only observation-family
noise on top of the exact fitted state. When \code{TRUE}, fresh
innovations are drawn from the trend's covariance structure
and added to the linpred, matching the marginal Monte Carlo
convention used by \code{posterior_epred()} /
\code{posterior_predict()} with \code{process_error = TRUE}.}
}
\value{
An object of class \link[=mvgam]{mvgam_forecast-class}.
Hindcasts are returned as a named list of \verb{[ndraws, n_times]}
matrices, one per series; \code{forecasts} / \verb{test_*} slots are
\code{NULL}.
}
\description{
Draw posterior hindcasts at the training time points. The
fitted latent state is read directly from the posterior draws
and the observation family then maps the trajectory back to
the response scale. For predictions at unseen times use
\link{forecast.mvgam}.
}
\examples{
\donttest{
set.seed(11)
simdat <- sim_mvgam(family = poisson(), n_series = 1L,
                     n_timepoints = 80L, trend_model = AR())

mod <- mvgam(
  y ~ s(x),
  trend_formula = ~ AR(p = 1),
  data    = simdat$data_train,
  family  = poisson(),
  chains  = 2, silent = 2
)

hc <- hindcast(mod)
plot(hc)
}

}
\seealso{
\link[=mvgam]{mvgam_forecast-class}, \link{forecast.mvgam},
\link{posterior_predict.mvgam} and \link{posterior_epred.mvgam} for the
alternative marginal-MC prediction surface that integrates
over the trend's stochastic dynamics instead of reading the
fitted latent state.
}
