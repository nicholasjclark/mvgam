% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidier_methods.R
\name{glance.mvgam}
\alias{glance.mvgam}
\title{One-row model summary for an \code{mvgam} fit}
\usage{
\method{glance}{mvgam}(x, looic = FALSE, ...)
}
\arguments{
\item{x}{A fitted \code{mvgam} object.}

\item{looic}{Logical. If \code{TRUE}, compute leave-one-out
information criteria via \code{\link[=loo.mvgam]{loo.mvgam()}} and add \code{elpd_loo},
\code{se_elpd_loo}, \code{p_loo} and \code{looic} columns. Defaults to
\code{FALSE} because computing \code{loo} can be slow on large fits.}

\item{...}{Forwarded to \code{\link[=loo.mvgam]{loo.mvgam()}} when \code{looic = TRUE}.}
}
\value{
A one-row tibble with columns:
\itemize{
\item \code{algorithm} -- the Stan algorithm used.
\item \code{pss} -- post-warmup sample size summed across chains.
\item \code{nobs} -- number of (non-missing) training observations.
\item \code{nseries} -- number of unique series in the training data.
\item \code{family} -- observation-family name.
\item \code{link} -- observation-family link.
\item Optional: \code{elpd_loo}, \code{se_elpd_loo}, \code{p_loo}, \code{looic}
(only when \code{looic = TRUE}).
}
}
\description{
broom-style one-row summary of an \code{mvgam} fit. Reports the
Stan algorithm, post-warmup sample size, training-observation
count, response family, link function and (optionally) \code{loo}
criteria. Mirrors \code{broom.mixed::glance.brmsfit()}.
}
\examples{
\donttest{
sim <- sim_mvgam(family = gaussian(), n_series = 1L,
                  n_timepoints = 50L, seed = 1L)
mod <- mvgam(y ~ s(season, bs = "cc"),
             trend_formula = ~ AR(p = 1),
             data = sim$data_train,
             family = gaussian(),
             chains = 1L, silent = 2L)
glance(mod)
glance(mod, looic = TRUE)
}

}
\seealso{
\code{\link[=tidy.mvgam]{tidy.mvgam()}}, \code{\link[=augment.mvgam]{augment.mvgam()}}, \code{\link[=loo.mvgam]{loo.mvgam()}}

Other tidiers: 
\code{\link{augment.mvgam}()},
\code{\link{tidy.mvgam}()}
}
\concept{tidiers}
