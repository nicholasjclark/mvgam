% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mvgam_core.R
\name{validate_newdata}
\alias{validate_newdata}
\title{Validate \code{newdata} against the training data of an \pkg{mvgam} model}
\usage{
validate_newdata(newdata, data)
}
\arguments{
\item{newdata}{A \code{data.frame} of prediction covariates in the same shape
as the training data (same factor levels and covariate columns).}

\item{data}{The \code{data.frame} (or \code{list}) used to fit the model, whose
\code{series} levels define the valid set.}
}
\value{
\code{newdata} with \code{series} coerced to the training factor levels,
returned invisibly, or \code{NULL} (invisibly) when \code{newdata} is \code{NULL}.
}
\description{
Runs the structural checks that \code{\link[=predict.mvgam]{predict.mvgam()}} and
\code{\link[=forecast.mvgam]{forecast.mvgam()}} apply to a \code{newdata} frame, so a misaligned frame is
caught up front rather than at the back of an expensive prediction
pipeline. It confirms that any \code{time} and \code{series} columns present in the
training \code{data} also appear in \code{newdata}, rejects \code{series} levels that
were not in the training data, and coerces \code{newdata$series} to the
training factor levels. Predictor and response columns are not enforced
here; the prediction functions resolve those from the model formula.
}
\examples{
train <- data.frame(
  series = factor(rep(c("a", "b"), each = 3L)),
  time = rep(1:3, times = 2L),
  y = rnorm(6L)
)
future <- data.frame(
  series = factor(rep(c("a", "b"), each = 2L), levels = c("a", "b")),
  time = rep(4:5, times = 2L)
)
validate_newdata(future, train)
}
\seealso{
\code{\link[=predict.mvgam]{predict.mvgam()}}, \code{\link[=forecast.mvgam]{forecast.mvgam()}}
}
