% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pp_average.mvgam.R
\name{pp_average.mvgam}
\alias{pp_average.mvgam}
\title{Model-averaged posterior predictions across \pkg{mvgam} fits}
\usage{
\method{pp_average}{mvgam}(
  x,
  ...,
  weights = "stacking",
  method = "posterior_predict",
  ndraws = NULL,
  nsamples = NULL,
  summary = TRUE,
  probs = c(0.025, 0.975),
  robust = FALSE,
  model_names = NULL,
  control = list(),
  seed = NULL
)
}
\arguments{
\item{x}{A fitted \code{mvgam} object. Further fits are passed via
\code{...}.}

\item{...}{Additional fitted \code{mvgam} objects to average over,
and (named) additional arguments passed through to
\code{\link[=as.data.frame.mvgam]{as.data.frame.mvgam()}} / \code{\link[=variables.mvgam]{variables.mvgam()}}.}

\item{weights}{Either a numeric vector with one entry per fit
(auto-normalised to sum to 1) or one of \code{"stacking"},
\code{"pseudobma"}, \code{"pseudobma+"}, \code{"loo"}, \code{"waic"}. See
\code{mvgam_loo_extras} for the per-strategy semantics.}

\item{method}{Which prediction primitive to call on each fit
before averaging. One of \code{"posterior_predict"} (the
default), \code{"posterior_epred"}, \code{"posterior_linpred"}.}

\item{ndraws}{Total number of model-averaged draws to return.
Defaults to the number of draws in the first fit.}

\item{nsamples}{Deprecated brms alias for \code{ndraws}.}

\item{summary}{Logical. If \code{TRUE} (the default), summarise the
averaged predictions with \code{\link[=posterior_summary.mvgam]{posterior_summary.mvgam()}} (mean
/ Est.Error / two quantiles); otherwise return the full
averaged draw matrix.}

\item{probs}{Numeric length-2 vector of quantile probabilities
for the summary path. Defaults to \code{c(0.025, 0.975)}.}

\item{robust}{Logical. If \code{TRUE}, summarise via median / MAD
rather than mean / SD on the summary path.}

\item{model_names}{Optional character vector naming the fits
in the same order they were passed.}

\item{control}{A list of extra arguments passed to
\code{\link[loo:loo_model_weights]{loo::loo_model_weights()}} when computing stacking /
pseudo-BMA weights.}

\item{seed}{Optional integer seed for reproducibility.}
}
\value{
A numeric matrix of averaged predictions. When
\code{summary = TRUE}, rows are observations and columns are
summary statistics; otherwise rows are draws and columns
are observations. Attributes \code{weights} and \code{ndraws} record
the per-model weight and integer draw count drawn from each
fit.
}
\description{
Returns Bayesian model-averaged posterior predictions from
two or more \code{mvgam} fits, in proportion to the supplied (or
computed) model weights. Mirrors \code{\link[brms:pp_average.brmsfit]{brms::pp_average.brmsfit()}}.
}
\examples{
\donttest{
sim <- sim_mvgam(family = gaussian())
mod_a <- mvgam(y ~ s(season, bs = "cc"), trend_model = AR(),
               data = sim$data_train, family = gaussian(),
               chains = 1, silent = 2)
mod_b <- mvgam(y ~ s(season, bs = "cc", k = 6),
               trend_model = AR(),
               data = sim$data_train, family = gaussian(),
               chains = 1, silent = 2)
pp_average(mod_a, mod_b, weights = "stacking")
}

}
\seealso{
\code{\link[brms:pp_average.brmsfit]{brms::pp_average.brmsfit()}},
\code{\link[=posterior_average.mvgam]{posterior_average.mvgam()}}, \link{mvgam_loo_extras}.
}
\author{
Nicholas J Clark
}
