% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare_scores.R
\name{plot.mvgam_compare_scores}
\alias{plot.mvgam_compare_scores}
\title{Plot per-model score trajectories from \code{compare_scores()}}
\usage{
\method{plot}{mvgam_compare_scores}(x, relative = NULL, facet = TRUE, ...)
}
\arguments{
\item{x}{A \code{mvgam_compare_scores} object from
\code{\link[=compare_scores]{compare_scores()}}.}

\item{relative}{Optional character name of a baseline model
already in \code{x$model}. When supplied, scores are differenced
against that model at each series x horizon cell and the
plot adds a dashed zero line; negative values then mean
"better than the baseline". The baseline model itself is
dropped from the plot since its values are identically
zero.}

\item{facet}{Logical. When \code{TRUE} (default) univariate scores
are faceted by series; when \code{FALSE} series are overlaid on
one panel with linetype distinguishing them. Ignored for
joint scores.}

\item{...}{Ignored.}
}
\value{
A \code{ggplot} object.
}
\description{
Draws a \code{ggplot2} line + point chart of forecast
scores from a \code{mvgam_compare_scores} object. The shape
adapts to the score type:
\itemize{
\item Univariate scores (\code{"crps"}, \code{"drps"}, \code{"logs"},
\code{"brier"}, \code{"sis"}, \code{"dss"}, \code{"qs"}, \code{"twcrps"}) plot
per-series with one facet panel per series; toggle off
with \code{facet = FALSE} to overlay every series on one
panel.
\item Joint multivariate scores (\code{"energy"},
\code{"variogram"}, \code{"twenergy"}) plot a single panel from
the \code{all_series} rows.
}
Ensembles included in the \code{compare_scores()} call appear
alongside the component models, so the same plot compares
individuals and ensembles in one go.
}
\seealso{
\code{\link[=compare_scores]{compare_scores()}}, \code{\link[=score.mvgam_forecast]{score.mvgam_forecast()}},
\code{\link[=plot.mvgam_compare_elpds]{plot.mvgam_compare_elpds()}},
\code{\link[=ensemble.mvgam_forecast]{ensemble.mvgam_forecast()}}
}
\author{
Nicholas J Clark
}
