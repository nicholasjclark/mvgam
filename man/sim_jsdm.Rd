% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_jsdm.R
\name{sim_jsdm}
\alias{sim_jsdm}
\title{Simulate multi-species data for a joint species distribution model}
\usage{
sim_jsdm(
  family = brms::negbinomial(),
  n_species = 5L,
  n_sites = 60L,
  n_lv = 2L,
  family_pars = list(),
  seed = NULL
)
}
\arguments{
\item{family}{Observation family. Any family mvgam's response
simulator draws from, plus \code{\link[=mvn]{mvn()}}, \code{\link[=mvt]{mvt()}}, \code{\link[=diri]{diri()}},
\code{\link[=multi]{multi()}} and \code{\link[=categ]{categ()}}. Defaults to \code{\link[brms:brmsfamily]{brms::negbinomial()}}.}

\item{n_species}{Number of species. Defaults to \code{5}.}

\item{n_sites}{Number of sites. Defaults to \code{60}.}

\item{n_lv}{Number of latent factors. Defaults to \code{2}.}

\item{family_pars}{Optional named list of family parameters:
\code{Psi} (per-species scale for \code{\link[=mvn]{mvn()}} / \code{\link[=mvt]{mvt()}}), \code{nu} (degrees
of freedom for \code{\link[=mvt]{mvt()}}), \code{phi} (concentration for \code{\link[=diri]{diri()}}),
\code{unit_total} (trial total per site for \code{\link[=multi]{multi()}}), and the
dispersion or scale parameter the remaining families take,
such as \code{size} for a negative binomial or \code{sigma} for a
gaussian.}

\item{seed}{Optional integer seed. The session's random state is
restored when the call returns.}
}
\value{
An object of class \code{mvgam_sim_jsdm}, inheriting
\code{mvgam_sim}, with elements:
\describe{
\item{\code{data_train}}{Long \code{data.frame} with columns \code{site},
\code{env}, \code{series} (the species), \code{y} and \code{time} (the unit
index \code{\link[=jsdgam]{jsdgam()}} keys on).}
\item{\code{data_test}}{\code{NULL}, kept for consistency with the other
simulators.}
\item{\code{y_array}}{\verb{[n_sites x n_species]} response matrix.}
\item{\code{family}}{The family used.}
\item{\code{n_species}, \code{n_sites}, \code{n_lv}}{The design.}
\item{\code{truth}}{List of the generative parameters: \code{loadings},
\code{lv}, \code{intercepts}, \code{env_slopes}, \code{env}, \code{residual_cov},
\code{residual_cor}, and the family parameters that were used.}
}
}
\description{
Draws a site-by-species response matrix from a latent factor model
and returns it in the long format \code{\link[=jsdgam]{jsdgam()}} takes. Each species
gets its own intercept and its own slope on a site-level
covariate, and the species are tied together by \code{n_lv} latent
factors whose loadings give the residual correlation between them.

This is the non-detection counterpart to \code{\link[=sim_closure_unit_data]{sim_closure_unit_data()}},
which simulates the same latent factor structure for the
occupancy and N-mixture families.
}
\details{
The linear predictor for species \code{k} at site \code{i} is
\code{intercept[k] + env_slope[k] * env[i] + Z[k, ] \%*\% lv[i, ]}, where
\code{Z} holds the loadings and \code{lv} the site scores. The residual
covariance the model targets is \verb{Z Z'}, plus a diagonal of squared
per-species scales for \code{\link[=mvn]{mvn()}} and \code{\link[=mvt]{mvt()}}.

How that predictor becomes a response depends on the family.
Families whose species are drawn independently, such as
\code{\link[brms:brmsfamily]{brms::negbinomial()}} or \code{\link[stats:family]{stats::poisson()}}, take one draw per
species. \code{\link[=mvn]{mvn()}} and \code{\link[=mvt]{mvt()}} add a per-species residual. The
composition families draw a site's species together: \code{\link[=diri]{diri()}}
takes gamma variates and normalises them, \code{\link[=multi]{multi()}} takes counts
against a fixed site total, and \code{\link[=categ]{categ()}} takes one species per
site as a one-hot row.

A composition family identifies its loadings only up to a shift
shared by the species, which is the subspace mvgam's
\code{sum_to_zero_vector} parameterisation covers. The loadings are
centred for those families, which puts the simulated truth in the
same subspace a fit explores.

How much of the factor structure survives into the response
depends on the observation noise. \code{\link[=mvn]{mvn()}} and \code{\link[=mvt]{mvt()}} take their
per-species scale from \code{Psi}, which defaults to 0.5 against
loadings drawn at 0.7. Every other family takes the scale its own
simulator defaults to, and a gaussian's is 1. Measured over eight
seeds at 100 sites, the agreement between the loadings-implied
residual correlation and the correlation present in the simulated
data is 0.96 for \code{\link[=mvn]{mvn()}} and drops as low as -0.44 for a gaussian
at that noise level. Pass \code{family_pars = list(sigma = 0.5)} for a
gaussian whose species correlations are recoverable.
}
\examples{
simdat <- sim_jsdm(
  family    = brms::negbinomial(),
  n_species = 4L,
  n_sites   = 30L,
  n_lv      = 2L,
  seed      = 1L
)
head(simdat$data_train)
summary(simdat)

# A composition family draws each site's species together
comp <- sim_jsdm(family = diri(), n_species = 4L, n_sites = 20L,
                 seed = 2L)
rowSums(comp$y_array)[1:3]

}
\seealso{
\code{\link[=jsdgam]{jsdgam()}} for the model this simulates from;
\code{\link[=sim_closure_unit_data]{sim_closure_unit_data()}} for the occupancy and N-mixture
counterpart; \code{\link[=sim_mvgam]{sim_mvgam()}} for time-series simulation.
}
