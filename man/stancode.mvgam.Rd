% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/print.mvgam.R
\name{stancode.mvgam}
\alias{stancode.mvgam}
\alias{stancode.mvgam_prefit}
\title{Extract Stan Code from mvgam Objects}
\usage{
\method{stancode}{mvgam}(object, ...)

\method{stancode}{mvgam_prefit}(object, ...)
}
\arguments{
\item{object}{A fitted \code{mvgam} object or \code{mvgam_prefit} object.}

\item{...}{Currently unused.}
}
\value{
A character string containing the Stan model code with class
\code{c("stancode", "character")}.
}
\description{
Extract the Stan model code used to fit mvgam objects. This function
follows brms conventions and returns a character vector with \code{stancode} class.
}
\details{
This method extracts the Stan code that was used to fit the model.
The returned object has the \code{stancode} class, making it compatible
with brms workflows and allowing users to inspect, modify, or save the
generated Stan code.
}
\examples{
\dontrun{
# Fit a simple mvgam model
data(portal_data)
mod <- mvgam(count ~ s(time), 
             trend_formula = ~ RW(), 
             data = portal_data, 
             family = poisson())

# Extract and inspect Stan code
code <- stancode(mod)
cat(code)

# Check class
class(code)
}

}
