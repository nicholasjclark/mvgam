% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trend_dispatcher.R
\name{custom_trend}
\alias{custom_trend}
\title{Create custom trend types}
\usage{
custom_trend(
  trend,
  tpars,
  forecast_fun,
  stancode_fun,
  standata_fun = NULL,
  bounds = list(),
  characteristics = list(),
  ...
)
}
\arguments{
\item{trend}{Character string naming the trend type}

\item{tpars}{Character vector of trend-specific parameter names}

\item{forecast_fun}{Character string naming the forecasting function}

\item{stancode_fun}{Character string naming the Stan code generation function}

\item{standata_fun}{Character string naming the Stan data preparation function}

\item{bounds}{Named list of parameter bounds for prior specification}

\item{characteristics}{Named list of trend characteristics and capabilities}

\item{...}{Additional parameters to store in the trend object}
}
\value{
A custom mvgam trend object
}
\description{
Allows users to define custom trend specifications. This is the main
extension point for adding new trend types.
}
\examples{
\dontrun{
# Create a custom GARCH trend type
GARCH <- function(p = 1, q = 1) {
  custom_trend(
    trend = "GARCH",
    tpars = c("alpha", "beta", "sigma_trend"),
    forecast_fun = "forecast_garch_rcpp",
    stancode_fun = "generate_garch_stan",
    standata_fun = "prepare_garch_data",
    bounds = list(
      alpha = c(0, 1),
      beta = c(0, 1),
      sigma_trend = c(0, Inf)
    ),
    characteristics = list(
      supports_predictors = TRUE,
      supports_correlation = FALSE,
      supports_factors = FALSE,
      max_order = max(p, q),
      requires_sorting = TRUE
    ),
    p = p, q = q
  )
}
}
}
