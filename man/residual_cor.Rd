% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/residual_cor.R
\name{residual_cor}
\alias{residual_cor}
\alias{residual_cor.mvgam}
\alias{residual_cor.jsdgam}
\title{Extract residual correlations from a fitted mvgam model}
\usage{
residual_cor(object, ...)

\method{residual_cor}{mvgam}(
  object,
  groups = FALSE,
  partial = FALSE,
  summary = TRUE,
  robust = FALSE,
  probs = c(0.025, 0.975),
  ...
)

\method{residual_cor}{jsdgam}(object, ...)
}
\arguments{
\item{object}{A fitted \link[=mvgam]{mvgam} object.}

\item{...}{Currently ignored.}

\item{groups}{Logical. Only relevant for hierarchical trends
(\code{gr = ...} supplied to the trend constructor). When \code{FALSE}
(default) the population-level correlation
(\code{tcrossprod(L_Omega_global)}) is returned. When \code{TRUE}, a named
list of \code{mvgam_residcor} objects is returned, one per group; each
group's correlation is reconstructed as
\code{alpha * tcrossprod(L_global) + (1 - alpha) * tcrossprod(L_dev[g])}.}

\item{partial}{Logical. When \code{TRUE}, also compute the residual
partial correlation matrix per draw via the inverse-correlation
identity \verb{P_ij = -inv(R)_ij / sqrt(inv(R)_ii * inv(R)_jj)}. This
is \code{O(p^3)} per draw and is omitted by default. Required if you
want the \verb{prec*} fields populated.}

\item{summary}{Logical. When \code{TRUE} (default) per-element summaries
(point estimate, posterior SD, quantile CI, posterior probability
mass) are returned. When \code{FALSE} the full per-draw arrays are
returned for downstream analysis.}

\item{robust}{Logical. When \code{FALSE} (default), the posterior mean
is the point estimate; when \code{TRUE}, the posterior median is used.
Only relevant when \code{summary = TRUE}.}

\item{probs}{Length-2 numeric vector giving the lower and upper
probabilities for the quantile-based credible interval. Default
\code{c(0.025, 0.975)}.}
}
\value{
An \linkS4class{mvgam_residcor} object when \code{summary = TRUE} and
\code{groups = FALSE}. When \code{groups = TRUE} on a hierarchical trend, a
named list of \code{mvgam_residcor} objects (one per group plus the
population-level matrix under the name \code{"_global"}). When
\code{summary = FALSE}, a list of per-draw arrays.
}
\description{
Compute residual (cross-series) correlation, covariance and partial
correlation summaries from a fitted \link[=mvgam]{mvgam} model whose
trend includes a correlated process error structure
(\code{cor = TRUE} on \code{RW()} / \code{AR()} / \code{ZMVN()}, or any \code{VAR()} /
\code{VARMA()} trend) or a hierarchical correlation structure via a
grouping variable on the trend.
}
\details{
For non-hierarchical correlated trends the per-draw correlation
matrix is reconstructed from the trend's posterior covariance:
\itemize{
\item Cholesky-scaled trends (\code{RW(cor = TRUE)}, \code{AR(cor = TRUE)},
\code{ZMVN(cor = TRUE)}) reconstruct
\code{Sigma = diag(sigma) \%*\% L_Omega \%*\% t(L_Omega) \%*\% diag(sigma)}
per draw.
\item Full-covariance trends (\code{VAR}, \code{VARMA}) use \code{Sigma_trend}
directly.
}
For hierarchical trends, per-group correlations combine the global
Cholesky factor and per-group deviations via
\code{alpha * tcrossprod(L_global) + (1 - alpha) * tcrossprod(L_dev[g])}.

Credible intervals and posterior SDs for correlations are computed
on the Fisher z-transformed scale (\code{atanh(r)}) and back-transformed
for display, which keeps the intervals well-behaved for
correlations near \verb{+/- 1}. Posterior probabilities
(\code{prob_positive}, \code{prob_negative}) are computed on the native
scale. \code{prob_nonzero = pmax(prob_positive, prob_negative)}
summarises one-sided evidence; \code{sig_cor} is the correlation matrix
with elements set to zero where \code{prob_nonzero <= 0.95} (the
threshold is exposed via the returned object's
\code{prob_threshold} metadata field).

Joint species distribution models (\code{jsdgam} fits) use a different
latent-factor parameterisation; this function will be wired up to
that case when the jsdgam port lands.
}
\references{
Hui, F. K. C. (2016). boral - Bayesian Ordination and
Regression Analysis of Multivariate Abundance Data in R.
\emph{Methods in Ecology and Evolution}, 7(6), 744-750.
\doi{10.1111/2041-210X.12514}
}
\seealso{
\linkS4class{mvgam_residcor}, \code{\link[=summary.mvgam_residcor]{summary.mvgam_residcor()}},
\code{\link[=print.mvgam_residcor]{print.mvgam_residcor()}}.
}
