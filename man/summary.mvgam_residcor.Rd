% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/residual_cor.R
\name{summary.mvgam_residcor}
\alias{summary.mvgam_residcor}
\title{Tidy summary of an \code{mvgam_residcor} object}
\usage{
\method{summary}{mvgam_residcor}(object, ...)
}
\arguments{
\item{object}{An \code{mvgam_residcor} object returned by
\code{\link[=residual_cor]{residual_cor()}} with \code{summary = TRUE}.}

\item{...}{Currently ignored.}
}
\value{
A \code{tibble::tibble} with columns \code{series_1}, \code{series_2},
\code{Estimate}, \code{Est.Error}, \code{Q_lower}, \code{Q_upper}, \code{ESS},
\code{prob_positive}, \code{prob_negative}, \code{prob_nonzero}, \code{sig} (whether
\code{prob_nonzero > prob_threshold}). \code{ESS} is the per-entry
effective sample size of the correlation draws on the Fisher-z
scale; low values flag pairs whose CI / point estimate look
tight but were poorly sampled by the chains.
}
\description{
Returns a tibble with one row per unique upper-triangle pair of
series, sorted by \code{prob_nonzero} descending (so the most credibly
non-zero pairs appear first).
}
