% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare_scores.R
\name{plot.mvgam_compare_elpds}
\alias{plot.mvgam_compare_elpds}
\title{Plot per-step LFO ELPDs from \code{compare_elpds()}}
\usage{
\method{plot}{mvgam_compare_elpds}(x, relative = NULL, cumulative = FALSE, ...)
}
\arguments{
\item{x}{A \code{mvgam_compare_elpds} object from
\code{\link[=compare_elpds]{compare_elpds()}}.}

\item{relative}{Optional character name of a baseline model
already in \code{x$model}. When supplied, per-step ELPDs are
differenced against that model at each evaluation
timepoint and the plot adds a dashed zero line; positive
values then mean "better than the baseline". The baseline
itself is dropped from the plot.}

\item{cumulative}{Logical. When \code{TRUE} (default \code{FALSE}) the
y-axis becomes the cumulative ELPD up to each evaluation
timepoint, which is the quantity \code{loo_compare()} sums
across folds. Useful for spotting when one model pulls
ahead of another over the rolling-origin window.}

\item{...}{Ignored.}
}
\value{
A \code{ggplot} object.
}
\description{
Draws a \code{ggplot2} line + point chart of
leave-future-out ELPDs over the rolling-origin evaluation
grid. Each model gets a coloured line. Refit timepoints
(where Pareto-k crossed the threshold and the model was
refit) are highlighted with a hollow marker so the reader
can see where mvgam stopped trusting the PSIS
approximation.
}
\seealso{
\code{\link[=compare_elpds]{compare_elpds()}}, \code{\link[=lfo_cv]{lfo_cv()}},
\code{\link[=loo_compare.mvgam_lfo]{loo_compare.mvgam_lfo()}},
\code{\link[=plot.mvgam_compare_scores]{plot.mvgam_compare_scores()}}
}
\author{
Nicholas J Clark
}
