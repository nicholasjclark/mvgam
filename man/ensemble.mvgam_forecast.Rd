% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ensemble.R, R/ensemble.mvgam_forecast.R
\name{ensemble.mvgam_forecast}
\alias{ensemble.mvgam_forecast}
\alias{ensemble}
\title{Combine forecasts from \pkg{mvgam} models into evenly weighted ensembles}
\usage{
ensemble(object, ...)

\method{ensemble}{mvgam_forecast}(object, ..., weights = NULL, ndraws = 5000L, seed = NULL)

ensemble(object, ...)

\method{ensemble}{mvgam_forecast}(object, ..., weights = NULL, ndraws = 5000L, seed = NULL)
}
\arguments{
\item{object}{An \code{mvgam_forecast} object returned by
\code{\link[=forecast.mvgam]{forecast.mvgam()}} or \code{\link[=hindcast.mvgam]{hindcast.mvgam()}}.}

\item{...}{More \code{mvgam_forecast} objects to combine.}

\item{weights}{Either \code{NULL} (the default, equivalent to even
weighting), or a numeric vector with one entry per supplied
forecast. Non-negative numeric weights are auto-normalised
so that they sum to 1. Strategy strings such as
\code{"stacking"} are intentionally not accepted here because
computing them requires the fitted models, not just the
forecast draws; pre-compute via
\code{\link[loo:loo_model_weights]{loo::loo_model_weights()}} / \code{\link[=loo_model_weights.mvgam]{loo_model_weights.mvgam()}}
on the fits and pass the numeric result.}

\item{ndraws}{Positive integer giving the total number of
ensemble draws to return per series. Defaults to \code{5000}.
If \code{ndraws} exceeds the total number of input draws,
sampling is done with replacement.}

\item{seed}{Optional integer seed for reproducibility.}
}
\value{
An object of class \code{mvgam_forecast} containing the ensemble
predictions. This object can be readily used with the supplied S3
functions \code{plot} and \code{score}.

An object of class \code{mvgam_forecast} containing the
ensembled hindcasts and forecasts. Suitable for
\code{\link[=plot.mvgam_forecast]{plot.mvgam_forecast()}} and \code{\link[=score.mvgam_forecast]{score.mvgam_forecast()}}.
Attribute \code{weights} records the per-model weights actually
used; attribute \code{ndraws_per_model} records the integer draw
counts drawn from each input.
}
\description{
Generate evenly weighted ensemble forecast distributions from
\code{mvgam_forecast} objects.

Build a single \code{mvgam_forecast} whose hindcast and forecast
draws are a multinomially-resampled combination of two or more
input \code{mvgam_forecast} objects. Defaults to even weighting, but
a numeric vector of user-supplied per-model weights is also
accepted (auto-normalised to sum to 1).
}
\details{
It is widely recognised in the forecasting literature that
combining forecasts from different models often yields more
accurate forecasts. The simplest way to create an ensemble is to use
evenly weighted combinations of forecasts from the different models.
This is straightforward to do in a Bayesian setting with \pkg{mvgam} as
the posterior MCMC draws contained in each \code{mvgam_forecast} object
will already implicitly capture correlations among the temporal posterior
predictions.

Per-model weights are split into integer draw counts
that sum to \code{ndraws} using the largest-remainder rule
(largest-remainder rule, after \code{brms::round_largest_remainder()}),
then that
many rows are sampled (with replacement when a model has
fewer draws than its assigned share) from each input's
hindcasts and forecasts. The returned object inherits its
\code{series_names}, \code{family}, \code{trend_model}, training arms and
test arms from the first input. All forecast objects must
share matching \code{series_names}, hindcast / forecast counts,
forecast horizons and (when present) test observations.
}
\examples{
\donttest{
# Fit two competing models on the same training data, ensemble
# their forecasts, and compare the per-horizon scores.
set.seed(11)
simdat <- sim_mvgam(family = poisson(), n_series = 1L,
                     n_timepoints = 120L, trend_model = AR(),
                     proportional_train = 0.75)

# Model 1: trend only.
mod1 <- mvgam(
  y ~ 1,
  trend_formula = ~ AR(p = 1),
  data    = simdat$data_train,
  newdata = simdat$data_test,
  family  = poisson(),
  chains  = 2, silent = 2
)

# Model 2: same trend, plus a smooth of x in the obs formula.
mod2 <- mvgam(
  y ~ s(x),
  trend_formula = ~ AR(p = 1),
  data    = simdat$data_train,
  newdata = simdat$data_test,
  family  = poisson(),
  chains  = 2, silent = 2
)

fc1 <- forecast(mod1, newdata = mod1$test_data)
fc2 <- forecast(mod2, newdata = mod2$test_data)
ens <- ensemble(fc1, fc2)

# Compare CRPS (lower is better).
mean(score(fc1, score = "crps")[[1L]]$score, na.rm = TRUE)
mean(score(fc2, score = "crps")[[1L]]$score, na.rm = TRUE)
mean(score(ens, score = "crps")[[1L]]$score, na.rm = TRUE)

plot(ens)
}

}
\seealso{
\code{\link{plot.mvgam_forecast}},
\code{\link{score.mvgam_forecast}}

\code{\link[=forecast.mvgam]{forecast.mvgam()}}, \code{\link[=hindcast.mvgam]{hindcast.mvgam()}},
\code{\link[=score.mvgam_forecast]{score.mvgam_forecast()}}, \code{\link[=pp_average.mvgam]{pp_average.mvgam()}},
\code{\link[=loo_model_weights.mvgam]{loo_model_weights.mvgam()}}. The online article
\url{https://nicholasjclark.github.io/mvgam/articles/forecast_evaluation.html}
shows \code{ensemble()} weighted by stacking weights derived
from \code{loo_model_weights.mvgam_lfo()}.
}
\author{
Nicholas J Clark
}
