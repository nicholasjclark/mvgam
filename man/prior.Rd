% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/priors.R
\name{prior}
\alias{prior}
\title{Set Prior Distributions for mvgam Model Parameters}
\usage{
prior(
  prior,
  class = "b",
  coef = "",
  group = "",
  resp = "",
  dpar = "",
  nlpar = "",
  lb = NA,
  ub = NA,
  source = "user"
)
}
\arguments{
\item{prior}{Character string defining the prior distribution in Stan syntax.
Examples: "normal(0, 1)", "exponential(2)", "uniform(-1, 1)"}

\item{class}{Parameter class. Standard brms classes ("b", "Intercept", "sd")
are routed to brms::prior(). Trend parameter classes with "_trend" suffix
are handled by mvgam system with validation.}

\item{coef}{Optional name of a specific coefficient within the parameter class.
Only used for classes that group multiple coefficients.}

\item{group}{Optional name of a grouping factor to specify group-level priors.}

\item{resp}{Optional name of response variable for multivariate models.}

\item{dpar}{Optional name of distributional parameter (e.g., "sigma", "shape").}

\item{nlpar}{Optional name of non-linear parameter for non-linear models.}

\item{lb}{Optional numeric lower bound for parameter. Use NA for unbounded.}

\item{ub}{Optional numeric upper bound for parameter. Use NA for unbounded.}

\item{source}{Character string indicating source of prior specification.
Usually "user" for explicitly set priors, "default" for package defaults.}
}
\value{
A \code{brmsprior} object (data frame) containing the prior
specification. For trend parameters, the object includes mvgam-specific
attributes for proper integration. Can be combined with other priors
using the \code{+} operator.
}
\description{
A wrapper around \code{brms::prior} that provides unified interface for
specifying prior distributions on both observation and trend model parameters.
Maintains full brms syntax compatibility while automatically detecting and
properly handling mvgam trend parameters.
}
\details{
This function provides several key advantages:

\subsection{Automatic Parameter Routing}{
\itemize{
\item \strong{Observation parameters}: Routed to \code{brms::prior()} with identical behavior
\item \strong{Trend parameters}: Validated and processed through mvgam system with
trend-specific constraints and validation
\item \strong{Detection}: Trend parameters identified by "_trend" suffix (e.g., "ar1_trend", "sigma_trend")
}
}

\subsection{Full brms Compatibility}{
\itemize{
\item Identical syntax and return structure to \code{brms::prior()}
\item All brms functionality preserved for observation model parameters
\item Seamless integration with existing brms workflows and functions
}
}

\subsection{Enhanced Trend Support}{
\itemize{
\item Validation of trend parameter class names against registered trend types
\item Constraint validation using \code{common_trend_priors} specifications
\item Integration with mvgam's centralized prior system
}
}
}
\section{Prior Combination}{

Multiple priors can be combined using the \code{+} operator:
\preformatted{
# Combine observation and trend priors
combined <- mvgam::prior("normal(0, 2)", class = "Intercept") +
            mvgam::prior("normal(0, 0.5)", class = "ar1_trend")
}
}

\section{Parameter Classes}{


\subsection{Standard brms Classes}{
All standard brms parameter classes are supported:
\itemize{
\item \code{"Intercept"}: Population-level intercepts
\item \code{"b"}: Population-level coefficients
\item \code{"sd"}: Standard deviations of group-level effects
\item \code{"cor"}: Correlations of group-level effects
}
}

\subsection{mvgam Trend Classes}{
Trend parameters use "_trend" suffix convention:
\itemize{
\item \code{"sigma_trend"}: Innovation standard deviations
\item \code{"ar1_trend"}, \code{"ar2_trend"}: AR coefficients by lag
\item \code{"rw_trend"}: Random walk parameters
\item And others depending on trend type registration
}
}
}

\examples{
\dontrun{
# Basic usage - observation model priors (delegated to brms::prior)
obs_intercept <- prior("normal(0, 2)", class = "Intercept")
obs_coef <- prior("normal(0, 0.5)", class = "b", coef = "x1")

# Trend model priors (handled by mvgam)
ar_prior <- prior("normal(0, 0.5)", class = "ar1_trend")
sigma_prior <- prior("exponential(2)", class = "sigma_trend")

# Combine multiple priors using + operator
all_priors <- obs_intercept + ar_prior + sigma_prior

# Use in mvgam model
fit <- mvgam(y ~ x1, trend_formula = ~ AR(p = 1), 
             data = data, prior = all_priors)
}

}
\seealso{
\code{\link{set_prior}}, \code{\link{get_prior.mvgam_formula}},
\code{\link[brms]{prior}}, \code{\link{mvgam_formula}}
}
