% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/birdsong.R
\docType{data}
\name{birdsong}
\alias{birdsong}
\title{Simulated weekly bird song counts with hierarchically correlated AR(1) trends}
\format{
A \code{data.frame} with 320 rows and the following fields:
\describe{
\item{series}{factor identifying the species (\code{warbler},
\code{thrush}, \code{wren}, \code{finch}); duplicated as \code{species} for
readability.}
\item{time}{integer week index from 1 to 80.}
\item{y}{integer weekly count (Poisson sampled); duplicated as
\code{count}.}
\item{species}{factor, alias of \code{series}.}
\item{week}{integer week index, alias of \code{time}.}
\item{week_in_year}{integer week within the annual cycle
(1..52), used for the shared seasonal smooth.}
\item{count}{integer weekly count, alias of \code{y}.}
}
}
\usage{
birdsong
}
\description{
A multivariate count time series for four bird species observed
weekly across 80 weeks. Each species has its own AR(1) latent
trend; the four series are coupled through a contemporaneous
innovation correlation matrix. Two species (warbler, thrush)
rise together; two (wren, finch) move opposite to them. A
shared cyclic seasonal smooth is added on week-within-year.
}
\details{
The dataset is designed so that an independent-AR fit
(\code{trend_formula = ~ AR(p = 1)}) treats the four trajectories as
unrelated, while \code{trend_formula = ~ AR(p = 1, cor = TRUE)}
recovers the cross-species correlation that drives the coupling.

True generative components:
\itemize{
\item Species random intercepts (centred) on the log scale.
\item Shared cyclic seasonal smooth (mgcv \code{bs = "cc"}) on
week-within-year.
\item Hierarchically correlated AR(1) trends with \code{phi = 0.7},
innovation standard deviation 0.35, and the 4 x 4 correlation
matrix grouping \verb{(warbler, thrush)} positively against
\verb{(wren, finch)}.
}

Recommended fit:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{mvgam(
  formula = y ~ s(week_in_year, bs = "cc", k = 8) +
                s(species, bs = "re"),
  trend_formula = ~ AR(p = 1, cor = TRUE),
  data = birdsong,
  family = poisson()
)
}\if{html}{\out{</div>}}

Built by \code{data-raw/build_birdsong.R}.
}
\keyword{datasets}
